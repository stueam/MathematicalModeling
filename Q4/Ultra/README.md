# Ultra 已迁移至 src/Q4

完整算法、依赖、测试、覆盖证书和实验材料已整体迁移至 [src/Q4](../../src/Q4/README.md)。算法与默认参数保持不变。

请在仓库 `src/Q4` 目录运行：

```powershell
python -X utf8 start_s21.py --self-test
```

`start_s21.py --connect` 仅用于问题四演练测试，不是正式测试入口。旧目录仅保留此迁移说明，避免维护两份算法源码。
