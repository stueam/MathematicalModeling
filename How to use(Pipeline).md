# 数学建模实战 Pipeline Note

> 目标：解决“单个算法会一点，但不知道怎样串成完整建模过程”的问题。本文不按教材章节堆知识，而按实际问题结构给出可以照着执行的 pipeline。

---

## 0. 所有题目共用的总 Pipeline

```text
读题与需求拆解
→ 识别任务类型和系统边界
→ 建立变量、约束、假设、情形四本账
→ 数据/附件审计
→ 建立最简单可运行基线 MVM
→ 选择与结构匹配的主模型
→ 参数估计或数值求解
→ V0–V6 多层验证
→ 必要时升级模型
→ 灵敏度、稳健性和情景分析
→ 转化为题目要求的方案、表格和结论
```

### 第一步：每问先填这张表

| 问题 | 已知输入 | 未知参数/状态 | 决策变量 | 目标 | 硬约束 | 要交付什么 | 依赖哪一问 |
|---|---|---|---|---|---|---|---|

### 第二步：判断问题属于哪条 Pipeline

| 题面动词/结构 | 跳转 Pipeline |
|---|---|
| 描述、比较、找规律 | 1 数据认识 |
| 分群、划分类别、识别类型 | 2 聚类/分类 |
| 找影响因素、预测连续值 | 3 回归预测 |
| 预测未来时刻 | 4 时间序列 |
| 综合评价、排名、选方案 | 5 评价决策 |
| 在约束下求最好方案 | 6–9 优化 |
| 分阶段行动、资源随时间变化 | 10 动态规划 |
| 路径、连接、运输、网络 | 11 图与网络 |
| 排班、调度、生产、车辆 | 12 调度与路径 |
| 随机需求、故障、排队 | 13 随机仿真 |
| 物理变化率、温度、运动、扩散 | 14 ODE/PDE |
| 根据观测反推出参数/结构 | 15 参数反演 |
| 定位、遮挡、轨迹、视线 | 16 几何建模 |
| 状态概率随时间转移 | 17 马尔可夫 |
| 等待、服务台、拥堵 | 18 排队论 |

### 第三步：始终保留一条简单基线

- 预测：均值/线性回归/上一时刻预测。
- 分类：多数类/Logistic/KNN。
- 优化：人工可行方案/贪心/小规模穷举。
- 动力系统：稳态或线性化模型。
- 评价：等权平均或单指标排序。

只有高级模型在精度、稳健性、解释性或计算量上明显更好，才保留它。

---

## 1. 数据认识与清洗 Pipeline

### 适用场景

题目给出 Excel/CSV/实验记录，后续需要分类、预测、评价或优化。

### Pipeline

```text
明确每行/每列含义
→ 单位和数据类型检查
→ 缺失、重复、异常审计
→ 时间/空间顺序检查
→ 指标方向和量纲处理
→ 探索性图表
→ 清洗前后对比
→ 输出可供后续模型使用的数据表
```

### 具体怎么做

1. 建立数据字典：变量名、意义、单位、类型、合法范围、是否可作为预测时使用。
2. 检查重复记录、空白、乱码、负数、零值、超出物理范围的值。
3. 缺失值处理：
   - 缺失很少且无规律：删除或局部插补；
   - 时间序列：前后插值、样条或状态空间；
   - 缺失与其他变量相关：回归/多重插补；
   - 缺失本身有意义：增加“是否缺失”特征。
4. 异常值处理：先判断是录入错误、真实极端事件还是另一种工况，不能机械删除。
5. 标准化：距离、聚类、PCA、SVM、神经网络前通常需要；树模型通常不强制。
6. 时间数据严禁随机打乱后再预测过去。

### 必须输出

- 数据字典；
- 缺失率和异常值表；
- 清洗规则；
- 清洗前后分布对比图；
- 最终样本量。

### 失败时怎么办

- 样本太少：降低模型复杂度，使用机理、Bootstrap 或小样本方法。
- 异常值很多：按工况/类别分组，不要强行用单一模型。
- 变量单位混乱：先统一单位，再进行标准化。

---

## 2. “清洗—分群—找特征—分类/预测”完整数据 Pipeline

这是最接近你所说的串联方式。

### Pipeline A：没有现成类别标签

```text
数据清洗
→ 特征构造与标准化
→ PCA/相关性去冗余
→ 聚类发现潜在群体
→ 选择聚类数并验证稳定性
→ 解释各群体的关键特征
→ 对不同群体分别建预测/优化模型
→ 比较“统一模型”和“分群模型”
```

例：任务定价数据。

```text
清洗位置、价格、完成情况
→ 构造距离、密度、时间等特征
→ 聚类形成中心区/边缘区/稀疏区
→ 用均值差异和特征重要性描述各类
→ 每类分别回归定价
→ 验证分群后预测误差是否下降
```

### Pipeline B：已有类别标签

```text
数据清洗
→ 防止标签泄漏
→ 分层划分训练/验证/测试集
→ 特征筛选与标准化
→ 简单分类基线
→ 树模型/SVM 等增强模型
→ 交叉验证和调参
→ 混淆矩阵、召回率、F1、AUC
→ 错分样本分析
→ 输出分类规则或概率
```

### 特征寻找不应独立进行

特征选择必须嵌入训练集内部：

```text
训练集内筛特征
→ 训练模型
→ 验证集调参
→ 测试集只做一次最终评价
```

否则会产生数据泄漏，测试结果会虚高。

### 什么时候不应先聚类

- 已有可靠标签，任务本身就是监督分类；
- 聚类后各组没有实际解释；
- 分群模型并未降低验证误差；
- 样本太少，分群后每组更少。

---

## 3. 回归、影响因素与连续值预测 Pipeline

### 适用场景

预测产量、浓度、价格、厚度、能耗、完成率，或解释哪些因素重要。

### Pipeline

```text
定义响应变量 y 和候选特征 X
→ 数据清洗与可视化
→ 训练/验证/测试划分
→ 线性回归基线
→ 残差、共线性和非线性诊断
→ 按问题升级模型
→ 交叉验证选择复杂度
→ 测试集最终评价
→ 特征重要性和效应解释
→ 预测区间与稳健性
```

### 模型升级路由

| 诊断结果 | 升级方法 |
|---|---|
| 共线性严重 | Ridge/Lasso/Elastic Net、PLS、PCA 回归 |
| 明显曲线关系 | 多项式、GAM、非线性回归 |
| 异方差/异常点 | 加权最小二乘、稳健回归、变量变换 |
| 预测优先、关系复杂 | 随机森林、XGBoost/LightGBM |
| 样本少、维数高 | 正则化、PLS，降低模型复杂度 |
| 输出是概率/类别 | Logistic 或分类 Pipeline |

### 验证

- 指标：MAE、RMSE、(R^2)，必要时 MAPE；
- 残差图：残差—拟合值、QQ 图、时间顺序残差；
- 特征：VIF、系数置信区间或特征重要性；
- 稳定性：Bootstrap 或交叉验证误差分布；
- 解释：控制其他变量后，特征变化对输出的影响。

### 必须输出

- 变量关系图；
- 基线与增强模型比较表；
- 残差图；
- 特征作用及方向；
- 预测值和区间。

---

## 4. 时间序列预测 Pipeline

### 适用场景

按日/月/年预测未来销量、温度、价格、流量、设备指标。

### Pipeline

```text
确认时间戳和采样频率
→ 缺失时刻和异常事件处理
→ 画原序列并分解趋势/季节
→ 设置滚动训练—验证窗口
→ 朴素预测基线
→ 平稳性和自相关诊断
→ 选择指数平滑/ARIMA/状态空间等
→ 滚动回测
→ 残差白噪声检验
→ 预测区间和情景修正
```

### 模型选择

- 只有水平变化：简单指数平滑；
- 趋势明显：Holt；
- 趋势 + 季节：Holt-Winters/SARIMA；
- 自相关结构明显：ARIMA；
- 多变量相互影响：VAR；
- 状态随时间变化、有噪声：Kalman/状态空间；
- 波动率本身变化：GARCH。

### 常见错误

- 随机切分时间序列；
- 用未来信息计算历史特征；
- 只展示拟合，不做滚动预测；
- 只给点预测，没有区间；
- 序列发生制度突变却仍用全历史平均。

---

## 5. 综合评价、排名与方案选择 Pipeline

### 适用场景

评价城市、企业、方案、设备、风险、发展水平。

### Pipeline

```text
明确评价对象和评价目的
→ 建立互不重复的指标体系
→ 指标正向化、无量纲化
→ 检查指标相关性和重复计权
→ 确定权重
→ 计算综合得分/距离
→ 排名与分档
→ 权重和标准化敏感性
→ 解释短板并给建议
```

### 权重选择

- 专家偏好明确：AHP/BWM；
- 只依赖数据离散程度：熵权；
- 同时考虑变异和冲突：CRITIC；
- 主客观都重要：组合赋权。

### 综合方法选择

- 离理想方案有多近：TOPSIS；
- 强调折中排序：VIKOR；
- 层级结构和专家判断：AHP；
- 信息模糊、等级边界不清：模糊综合评价。

### 决定论文质量的步骤

1. 删除高度重复指标或用 PCA 降维；
2. 解释权重来源；
3. 改变权重 ±10% 检查排名是否翻转；
4. 与等权、另一种权重法或实际结果对照。

---

## 6. 线性规划 LP Pipeline

### 适用场景

收益/成本和所有约束均为线性，变量可连续。

### Pipeline

```text
定义决策变量和单位
→ 写目标函数
→ 建约束账本
→ 检查线性、方向和上下界
→ 小规模手算/人工可行解
→ LP 求解
→ 回代全部约束
→ 分析松弛量和影子价格
→ 参数敏感性与方案解释
```

### 重点

- 每个题目条件必须对应一条约束或说明为何不进入模型；
- 绝对值和 max/min 用辅助变量线性化；
- 变量若实际上必须为整数，不能用连续 LP 后四舍五入；
- 无界通常表示漏了资源或变量边界；不可行通常表示约束冲突。

### 输出

- 最优决策变量表；
- 目标值；
- 各约束余量；
- 瓶颈资源；
- 资源变化后的边际收益。

---

## 7. 整数规划与 0-1 规划 Pipeline

### 适用场景

选择/不选、建设/不建设、分配人员、车辆数量、班次、路径边、设备启停。

### Pipeline

```text
识别离散决策
→ 定义整数/0-1 变量
→ 写资源和逻辑约束
→ 尽量缩紧变量上界和 Big-M
→ 求 LP 松弛作基线
→ MILP/CP-SAT 求解
→ 检查最优界和求解间隙
→ 回代可行性
→ 与贪心/人工方案对比
```

### 逻辑约束模板

- B 依赖 A：(x_B\le x_A)；
- A、B 互斥：(x_A+x_B\le1)；
- 至少选一个：\(\sum x_i\ge1\)；
- 选择后才允许生产：(q_i\le Mx_i)。

### 何时换 CP-SAT

- 大量排班、先后顺序、时间区间和逻辑条件；
- 目标和约束以整数、布尔关系为主；
- Big-M 非常难取。

---

## 8. 连续非线性优化 Pipeline

### 适用场景

连续尺寸、角度、质量、速度、功率等参数优化，目标或约束非线性。

### Pipeline

```text
列变量、范围和单位
→ 找单调性、凸性和可分解结构
→ 构造可行初值
→ 网格/单变量搜索形成基线
→ SQP/内点法局部优化
→ 多初值重复
→ 检查约束余量和 KKT/梯度
→ 全局算法搜索
→ 用局部算法精修
→ 灵敏度和极限工况
```

### 算法选择

- 单调一维：二分/Brent；
- 光滑约束：SQP/内点；
- 无约束光滑：BFGS/L-BFGS；
- 不光滑、黑箱：模式搜索/DE/CMA-ES；
- 单次评价昂贵：代理模型/贝叶斯优化。

### 可信度要求

如果不能证明全局最优，应写“多初值和全局搜索范围内得到的最好可行解”，并报告搜索范围、重复次数和基线差距。

---

## 9. 多目标优化 Pipeline

### 适用场景

成本、效率、安全、风险、稳定性等目标互相冲突。

### Pipeline

```text
区分硬约束和真正目标
→ 明确各目标方向和单位
→ 分别求每个单目标最优值
→ 归一化目标
→ 生成 Pareto 解集
→ 观察权衡关系
→ 根据偏好/阈值选择推荐解
→ 权重或阈值敏感性
→ 给保守/均衡/激进方案
```

### 方法选择

- 有明确主目标：ε-约束；
- 有严格优先级：词典序/分层优化；
- 希望展示完整权衡：Pareto、NSGA-II、MOEA/D；
- 目标简单且偏好可靠：归一化加权和。

### 必须输出

- 单目标极值表；
- Pareto 图；
- 推荐点选择依据；
- 至少三种偏好下的方案；
- 结论是否随权重翻转。

---

## 10. 动态规划 Pipeline

### 适用场景

多阶段、当前选择影响未来状态，例如补给、库存、投资、路径、生产计划。

### Pipeline

```text
划分阶段
→ 定义最少充分状态
→ 定义动作和可行动作集合
→ 写状态转移
→ 写单阶段成本/收益
→ 给终端条件
→ 递推求值
→ 保存策略并回溯路径
→ 与贪心和小规模穷举对照
→ 状态离散步长敏感性
```

### 状态设计检验

问自己：“只知道当前状态，不知道完整历史，是否足以计算下一阶段？”如果不能，状态缺信息；如果包含大量不会影响未来的历史，状态过度。

### 升级路线

- 状态连续：离散化或插值；
- 状态高维：压缩、剪枝、近似 DP；
- 信息随机：随机 DP/MDP；
- 时间很长：滚动优化。

---

## 11. 图与网络 Pipeline

### 适用场景

路线、连接、运输、通信、匹配、流量、覆盖。

### Pipeline

```text
确定节点和边的现实含义
→ 决定有向/无向、容量、费用和权重
→ 构造邻接表/矩阵
→ 检查连通性和异常边
→ 选择图算法
→ 回溯具体路径/匹配
→ 用小图手算验证
→ 解释瓶颈、关键节点和方案变化
```

### 路由

- 非负最短路：Dijkstra；
- 全点对最短路：Floyd；
- 启发式空间路径：A*；
- 最低连接成本：MST；
- 容量最大：最大流；
- 流量同时有费用：最小费用流；
- 人—任务一一分配：Hungarian/KM；
- 网络瓶颈：最小割；
- 有依赖顺序：拓扑排序/关键路径。

---

## 12. 调度、排班、车辆路径 Pipeline

### 适用场景

生产调度、机器作业、人员排班、配送、车辆路径、带时间窗服务。

### Pipeline

```text
列对象、资源、时间和任务
→ 写工序先后与资源冲突
→ 定义目标：完工时间/成本/迟期等
→ 构造贪心或规则基线
→ MILP/CP-SAT 建模
→ 小规模精确求解
→ 大规模启发式/元启发式
→ 甘特图或路径图
→ 随机故障/需求下仿真
→ 滚动重调度与稳健性
```

### 方法选择

- 规则简单、规模小：Johnson/精确 MILP；
- 时间区间和逻辑多：CP-SAT；
- VRP/VRPTW：OR-Tools、MILP、局部搜索；
- 规模很大：GA、禁忌、模拟退火、VNS；
- 切割下料：模式生成 + 列生成。

### 输出

- 甘特图/车辆路线图；
- 资源利用率；
- 完工时间、迟期和成本；
- 与基线的改善百分比；
- 故障情景下可行率。

---

## 13. 随机系统、蒙特卡洛与离散事件仿真 Pipeline

### 适用场景

随机需求、故障、成功率、天气、服务时间、收益风险、复杂系统无法解析计算。

### Pipeline

```text
识别随机量
→ 由数据拟合分布或设置情景
→ 明确随机变量相关性
→ 建立单次仿真机制
→ 固定随机种子做小样例验证
→ 批量重复仿真
→ 输出均值、分位数、置信区间
→ 方差缩减/增加样本直到稳定
→ 将仿真嵌入优化
→ 最坏/压力情景测试
```

### 蒙特卡洛不是“随机跑几次”

必须说明：

- 分布来源；
- 重复次数；
- 随机种子；
- 估计量；
- 标准误/置信区间；
- 收敛证据。

### 仿真优化

```text
候选决策 x
→ 对 x 重复仿真
→ 得到期望成本、风险、可行率
→ 优化器更新 x
→ 对最终方案用更大样本独立复核
```

---

## 14. ODE/PDE 机理建模 Pipeline

### 适用场景

运动、温度、扩散、浓度、人口、传染、振动、压力、波浪等随时间/空间变化的问题。

### Pipeline

```text
画系统和作用关系图
→ 选状态变量与系统边界
→ 写守恒/受力/速率机制
→ 做量纲和数量级检查
→ 给初值、边界和参数
→ 先求简化/稳态模型
→ 数值离散求解
→ 参数拟合或反演
→ 步长/网格收敛
→ 与实验、守恒量和极端情形验证
→ 在机理模型上优化设计参数
```

### ODE 路由

- 一般非刚性：自适应 RK；
- 刚性系统：BDF/Radau；
- 边值问题：shooting 或边值求解器；
- 参数未知：ODE 求解器嵌入最小二乘。

### PDE 路由

- 规则区域、简单边界：有限差分；
- 守恒流动：有限体积；
- 复杂几何：有限元；
- 热传导：显式、隐式或 Crank–Nicolson。

### 决定性验证

- 单位是否一致；
- 总量是否守恒；
- 步长减半结果是否稳定；
- 边界条件变化是否符合物理直觉；
- 参数变化是否产生合理方向。

---

## 15. 参数辨识与反演 Pipeline

### 适用场景

已观测到输出，需要反求厚度、速度、热参数、阻尼、反应速率、设备参数或隐含结构。

### Pipeline

```text
建立前向模型 y=f(theta, x)
→ 确认观测量和未知参数
→ 检查参数可识别性
→ 定义误差模型和损失函数
→ 给合理参数范围和初值
→ 最小二乘/极大似然求解
→ 多初值或全局搜索
→ 残差和参数相关性检查
→ Bootstrap/轮廓似然给区间
→ 用独立数据前向预测验证
```

### 可识别性预警

若多组参数都能给出几乎相同输出，则不能只报告一组精确参数。应：

- 固定部分已知参数；
- 加入不同工况的数据；
- 加正则化或先验；
- 报告参数区间和相关性。

### 典型损失

- 高斯误差：加权最小二乘；
- 相对误差稳定：相对残差或对数误差；
- 有异常点：Huber/稳健损失；
- 多数据源：按测量不确定度加权。

---

## 16. 几何、定位、遮挡和轨迹 Pipeline

### 适用场景

无人机定位、CT、测线、视线、烟幕遮蔽、定日镜、机械链条、空间路径。

### Pipeline

```text
统一坐标系和时间基准
→ 参数化对象的位置/姿态
→ 写距离、角度、投影和相交关系
→ 建立情形树：相交/相切/分离等
→ 用简单几何算例验证
→ 根据观测反求参数
→ 对路径/姿态/位置优化
→ 动画或多时刻可视化
→ 边界和退化情形检查
```

### 关键提醒

- 角度单位统一使用弧度或明确转换；
- 三维旋转注意旋转顺序；
- 遮挡判定需要线段而非无限直线；
- 相切、共线、零距离需要容差；
- 优化前先把几何可行条件写清楚。

---

## 17. 马尔可夫链与状态转移 Pipeline

### 适用场景

品牌转移、设备状态、天气状态、人口流动、等级变化、长期占比。

### Pipeline

```text
定义互斥且完备的状态
→ 从连续记录统计转移次数
→ 估计并平滑转移矩阵
→ 检查每行和为 1
→ 检查时间齐次性
→ 计算多步分布
→ 求稳态/吸收概率和时间
→ 与实际留出时期对比
→ 对转移概率做敏感性分析
```

### 升级条件

- 状态不可直接观测：HMM；
- 行动影响转移：MDP；
- 转移概率随时间变化：非齐次马尔可夫链；
- 样本少：贝叶斯/平滑估计。

---

## 18. 排队论 Pipeline

### 适用场景

窗口、医院、服务器、维修、交通入口、生产等待。

### Pipeline

```text
定义顾客、服务台和队列规则
→ 估计到达率和服务率
→ 检验分布假设
→ 选择 M/M/1、M/M/s 等基线
→ 检查稳态条件
→ 计算等待和利用率指标
→ 用离散事件仿真验证
→ 把等待成本与服务成本联合优化
→ 高峰、故障和有限容量压力测试
```

### 何时必须改用仿真

- 到达/服务不是指数分布；
- 有优先级、插队、批量到达；
- 多队列共享资源；
- 服务台会故障或换班；
- 系统非稳态、存在明显高峰。

---

## 19. 机器学习 Pipeline

### 适用场景

数据量足够、输入输出明确、关系复杂，主要目标是预测或分类。

### Pipeline

```text
明确标签和可用特征
→ 按真实使用场景切分数据
→ 预处理只在训练集拟合
→ 简单统计/线性模型基线
→ 选择树模型/SVM/神经网络
→ 交叉验证和调参
→ 测试集一次性评价
→ 特征重要性/部分依赖解释
→ 错误案例与不确定性分析
→ 与机理或决策模型连接
```

### 连接后续决策

预测通常不是终点：

```text
预测需求/风险/成功概率
→ 将预测值或分布作为优化模型输入
→ 求决策方案
→ 在预测误差情景下复核方案
```

例如：

```text
预测各地区需求
→ VRP 配送优化
→ 蒙特卡洛模拟需求误差
→ 调整安全库存和车辆数量
```

### 模型选择

- 小样本、解释优先：Logistic/线性回归；
- 表格数据：随机森林、XGBoost/LightGBM；
- 高维稀疏：正则化线性模型/SVM；
- 图像、序列且数据量大：CNN/LSTM/Transformer；
- 无标签：聚类、异常检测。

---

## 20. 从预测连接到优化的 Pipeline

这是比赛中最容易断开的地方。

### 错误做法

```text
建立预测模型
→ 报 RMSE
→ 结束
```

### 正确做法

```text
预测未来输入及不确定性
→ 将预测结果输入决策模型
→ 求确定性最优方案
→ 在多个预测情景下重算
→ 统计方案成本、风险和可行率
→ 必要时改成鲁棒/机会约束模型
→ 输出可执行方案
```

### 三种连接方式

1. **点预测 → 确定性优化**：简单，但忽略预测误差。
2. **情景预测 → 随机规划**：对多个未来场景求期望最优。
3. **预测区间 → 鲁棒优化**：要求区间内仍可行，方案更保守。

### 验证

最终评价不只看预测误差，还要看：

- 决策成本；
- 约束违反率；
- 最坏情形损失；
- 与完美信息最优解的差距；
- 与简单预测基线产生的决策差距。

---

## 21. 从机理模型连接到优化的 Pipeline

### Pipeline

```text
建立机理方程
→ 用实验数据校准参数
→ 验证机理模型
→ 定义可控设计变量
→ 把机理求解器作为目标/约束计算器
→ 先做参数扫描理解结构
→ 再做连续/多目标优化
→ 极限工况和敏感性分析
→ 给推荐设计与安全裕度
```

例如回焊炉：

```text
热传导模型
→ 拟合传热参数
→ 预测炉温曲线
→ 计算峰值温度、升温斜率、液相时间
→ 优化传送速度和温区设定
→ 检查全部工艺约束
→ 对环境温度和参数误差做稳健性分析
```

---

## 22. 从评价连接到优化的 Pipeline

### Pipeline

```text
建立评价指标和权重
→ 计算当前方案得分
→ 找出各对象短板
→ 将得分函数或关键指标转为优化目标
→ 把最低等级要求写成硬约束
→ 优化资源分配/改进方案
→ 重新评价优化后的方案
→ 权重扰动下复核
```

注意：评价模型负责“衡量好坏”，优化模型负责“决定怎么做”。不能只排名而不给行动方案。

---

## 23. 验证 Pipeline：每个模型最后都必须经过

```text
V0 实现验证
→ V1 数学一致性
→ V2 数值可靠性
→ V3 数据解释能力
→ V4 机制与边界
→ V5 决策稳健性
→ V6 独立方法对照
```

| 层级 | 要做什么 |
|---|---|
| V0 | 小样例、手算、单元测试 |
| V1 | 单位、守恒、残差、约束余量 |
| V2 | 步长、网格、多初值、多随机种子、最优界 |
| V3 | 留出集、交叉验证、残差、置信区间 |
| V4 | 极端值、退化情形、反事实、物理常识 |
| V5 | 参数扰动、情景、最坏情况、可行率 |
| V6 | 解析 vs 数值、专用算法 vs 通用求解器 |

每个核心结论至少覆盖三层验证；决定推荐方案的结论尽量覆盖五层。

---

## 24. 一个可以直接复制到比赛文档中的“逐问 Pipeline 模板”

```markdown
### 问题 X

#### 1. 输入与输出
- 输入：
- 待求参数/状态：
- 决策变量：
- 输出：

#### 2. 结构判断
- 属于：预测 / 评价 / 优化 / 动力系统 / 几何 / 随机系统
- 决定性结构：守恒 / 状态转移 / 凸性 / 单调性 / 网络 / 随机性

#### 3. 假设与约束
- 假设：为什么需要、偏差方向、如何验证
- 硬约束：公式、来源、容差
- 情形：正常、边界、极端、不可行

#### 4. 简单基线
- 模型：
- 算法：
- 结果：
- 不足：

#### 5. 主模型
- 方程/目标：
- 参数估计：
- 求解算法：
- 初值、范围、停止条件：

#### 6. 验证
- V0：
- V1：
- V2：
- V3/V4：
- V5：

#### 7. 输出与解释
- 关键数值及单位：
- 适用条件：
- 误差/区间：
- 现实建议：
```

---

## 25. 比赛时的停止与换路规则

遇到以下情况，不要继续堆复杂算法：

- 两小时内无法在小样例跑通；
- 参数不可识别，结果强依赖初值；
- 高级模型相对基线没有稳定提升；
- 只能输出一个黑箱数字，无法验证；
- 单次仿真太慢，无法支持全局搜索；
- 复杂模型破坏了物理约束或可解释性。

换路顺序：

```text
复杂模型失败
→ 回到简单基线
→ 寻找结构降维
→ 简化变量/情形
→ 使用可解释专用算法
→ 把复杂模型放在改进与展望
```

---

## 26. 最小熟练度：不是背算法，而是能独立走完闭环

对每条 Pipeline，你真正需要练到的是：

1. 看题后能在 10 分钟内判断是否适用；
2. 能写出输入—状态—决策—约束—输出；
3. 能先做一个简单基线；
4. 能说明为什么升级模型；
5. 能跑出结果并回代验证；
6. 能给敏感性、误差或置信区间；
7. 能把数字翻译成现实建议。

> 一套可靠的建模 Pipeline 不是“数据清洗后连续调用很多算法”，而是每一步都根据上一阶段的诊断决定下一步，并且最终形成：题目机制 → 可计算模型 → 算法 → 验证 → 决策的闭环。
