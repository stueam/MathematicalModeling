# 论文代码清理验证

## 审查修复

针对 `0056aff` 的审查已修复队号脱敏破坏数值、S21 独立检查入口路径失效，以及论文案例导入与当前 runner 不兼容的问题。Q4 演练日志共用修正后的结构化脱敏；Q3/Q4 的正式传输模块仍逐字相同。当前格式检查和新增入口检查已纳入 `verify.py`。

论文表格的历史核验入口迁到 [Q3/evidence/paper_results](../Q3/evidence/paper_results/README.md)，直接读取原批次归档，保留历史数值及哈希。三组正文未引用的旧图共 14 个文件、1,959,165 字节已移出当前树，完整内容可从 `0056aff` 恢复；正文的 12 张图片及冻结案例数据保持原样。

本次单元与协议检查为 Q2 12、Q3 88、Q4 56，共 156 项，覆盖队号数字与坐标/时间碰撞、原始反馈保留、Q4 演练日志和证书默认 CLI。格式整理前后，所有数值算法、物理内核和本地 runner 的 AST 相同（忽略文档字符串），没有调整算法参数。

本次独立验证记录见 [evidence/review_fixes_validation.json](evidence/review_fixes_validation.json)，列出当前源码哈希、完整验证步骤、本地任务成绩及论文导入核验。下方的 64+30 局是 `0056aff` 的既有验证，保留原版本和原始哈希，不将它们记成本次新实验。

完整 `verify.py` 共 18 步全部通过。Q2 两组完整网格结果与下表一致；Q3 seed 0 为 15/15 清除、2738.940927s，Q4 seed 800 为 10/10 清除、5720.143045s。两轮合计 414 个动作与修复前逐条核对，动作、请求 ID、公开反馈和虚拟时间相同，排除规划耗时和现实时间戳。论文导入另验证了旧文件格式加独立源码目录、导出新目录、拒绝覆盖既有目录，以及错误源码哈希在导出前被拒绝。

## 清理版本历史记录：0056aff

2026-09-13，依据 `essay/essay.tex` 完成 `src` 清理。Q2 只保留固定 R0 贝叶斯选点；Q3、Q4 各只有一个论文策略类，所有运行入口共用对应实现。Python 文件由 71 个减至 52 个；旧策略类、消融开关、S22 比较、内部分析脚本及过期清单已删除。

该版本的完整机器记录见 [evidence/paper_validation.json](evidence/paper_validation.json)，包含论文与代码 SHA-256、逐局成绩、动作轨迹摘要和验证步骤。其哈希对应 `0056aff`，不会随本次格式整理改写。安装版本固定在 `requirements-tested.txt`。

## 数值与完整任务

| 验证项目 | 实测结果 |
| --- | --- |
| Q2 对称完整网格 | 最佳点 (840, 495)，J=23.14769634m |
| Q2 非对称完整网格 | 最佳点 (1140, 945)，J=12.93221148m |
| Q3 论文地图 | 64/64 局完整完成，清除 857 个源，均值 2856.512372s / 10531.702484m |
| Q4 论文地图 | 30/30 局完整完成，清除 383 个源，均值 5694.319195s / 20384.095968m |
| 单元与协议测试 | Q2 12、Q3 84、Q4 50，共 146 项通过 |
| 运行模块检查 | Q2 2、Q3 18、Q4 20，全部由显式运行入口可达 |
| 静态检查 | Ruff、Vulture 100% 置信检查通过；60% 运行代码检查仅出现由 requests 内部读取的 `trust_env` 属性 |
| S21 布局 | 整数证书、当前几何环境的全域覆盖、自检通过 |

Q3、Q4 各选 3 组完整任务，与清理前提交 `d92ddd1` 的论文策略逐动作比较，共 1437 个动作。动作坐标、频道、类型、请求 ID、公开反馈和虚拟时间全部一致；实际运行时间戳不参与比较。

Q3 的后备网格、Q4 的保守覆盖后备均属于论文执行流程，已合并进唯一策略。概率积分失效不会证明目标不存在，清除 10 个源不会触发完成。

这些是该版本锁定数值环境的实测结果；不同 NumPy/SciPy/Shapely 版本可能使候选比较出现微小差异。未改写论文中的历史实验数字。正式入口通过本地模拟 HTTP、丢包重试和全任务动作一致性测试；上述验证没有连接官方模拟器。

## 重跑

在 `src` 下执行：

```bash
python -m pip install -r requirements-tested.txt
python verify.py
python Q3/run.py reproduce --workers 4
python Q4/run.py reproduce --workers 4
```
