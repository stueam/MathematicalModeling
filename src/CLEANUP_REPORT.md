# src 清理验证记录

2026-09-13，基于远端 `cba9db1` 的源码整理，并同步了后续仅增加正式日志文件的 `d54cc2e`。修改范围为 `src`。全部核验使用本地模拟器或模拟 HTTP，没有启动官方演练或正式测试。

## 清理内容

- 删除 19 个旧 Python 文件，主要为 Q3 未接入的 MC 规划/采样模块、Q4 交付版本未使用的实验分支及旧 Q3 巡逻实现；另删除两个空占位文件。Q3 移动基线仍需的确定性似然提取到 `likelihood.py`。
- 删除未调用的候选生成方法、状态副本、统计属性、导入和变量；Q4 的 probes 测点计算直接归入最终策略，保留完整覆盖、保守几何、固定误差和有限完成后备。
- Q4 不完整运行返回失败退出码；本地运行与布局比较支持新输出目录。运行清单按实际源码生成，避免删除文件后出现哈希读取失败。
- Q3 原生 Windows 演练修复 PowerShell 路径；正式与演练复用同一个适配器。S21 启动器使用明确的参数解析，互斥模式不能混用。
- Q2 拒绝非有限坐标和无效步长，补齐 CSV/JSON 完整搜索验证。增加统一依赖、固定环境版本、源码引用检查及 `verify.py`。

包含新增测试和验证程序后，Python 文件由 **86 个降至 71 个**，代码行数由 **11,322 行降至 9,329 行**，净减少 **1,993 行**。历史证据和源文件迁移哈希保留，不作为当前源码清单。

## 验证结果

在全新 CPython 3.12.3 环境安装依赖，版本见 `requirements-tested.txt`。

| 检查 | 实测结果 |
| --- | --- |
| Q2 / Q3 / Q4 测试 | 12 / 109 / 62 通过，合计 **183 项** |
| 静态检查 | Ruff 通过；Vulture 100% 置信检查通过 |
| 源码引用 | 从实际入口与测试追踪普通导入、相对导入和 vendor 动态加载；未发现孤立模块 |
| Q2 默认完整搜索 | 两种情形各 2,821 个粗网格点；对称细化 338 点，不对称细化 169 点；CSV/JSON 完整生成 |
| Q3/Q4 清理前后等价 | 6 组完整任务，**1,437 条动作与反馈逐条相同**；只排除实际时间戳及规划耗时字段 |
| Q3 多进程配对入口 | 两个种子、baseline / bayes-fast，共 4/4 完成 |
| Q4 S22/S21 布局配对 | 30 组、60/60 完成；S21 平均虚拟时间 5,694.319195 s，S22 为 5,859.000997 s |
| 独立交付 | 单独复制 Q2、Q3、Q4 后，Q2 单点与 Q3/Q4 完整本地入口成功，不依赖仓库旧目录 |
| 论文历史数值 | 复核 64 条既有记录、费用恒等式和正文数值；未冒充新跑的 64 局结果 |
| 正式与演练入口 | 帮助入口可用；模拟 HTTP 测试覆盖就绪检查、同 ID 重试、停止规则、日志和完整动作流程 |

Q2 推荐点保持为：对称 `(840, 495)`，目标值 `J=23.14769633928804 m`；不对称 `(1140, 945)`，`J=12.932211482338602 m`。

本次等价对照的原始与清理后源码均使用同一个环境：

| 程序 | 场景 / 种子 | 清除数 | 虚拟时间（秒，前后相同） | 动作数 |
| --- | --- | ---: | ---: | ---: |
| Q3 | uniform / iid / 0 | 16 | 2738.940927 | 120 |
| Q3 | boundary / extreme / 10000 | 10 | 3255.050512 | 165 |
| Q3 | near / correlated / 10001 | 16 | 2934.925921 | 112 |
| Q4 | uniform / iid / 800 | 10 | 5720.143045 | 294 |
| Q4 | boundary / extreme / 10000 | 10 | 6302.909902 | 357 |
| Q4 | outward / correlated / 10001 | 16 | 6851.105267 | 389 |

数值库版本会影响浮点计算及临界候选选择，因此本次结果不覆盖旧环境的冻结论文统计。保留的算法与同环境原代码逐动作一致。Windows 官方软件的真实正式接入未在本次触发，现有正式入口行为由模拟协议测试验证。

当前源码哈希、删除清单和详细指标见 [机器可读验证记录](evidence/cleanup_validation.json)。重新核验在 `src` 运行 `python verify.py`；每次产生独立日志目录。
