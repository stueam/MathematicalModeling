\documentclass[UTF8,a4paper,12pt]{ctexart}

% 页面设置
\usepackage{geometry}
\geometry{
  left=2.5cm,
  right=2.5cm,
  top=2.5cm,
  bottom=2.5cm
}

% 数学公式
\usepackage{amsmath,amssymb}

% 插入图片
\usepackage{graphicx}
\usepackage{placeins}

% 表格
\usepackage{booktabs}
\usepackage{tabularx}

% 超链接
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=black]{hyperref}

\title{\textbf{几何约束下基于贝叶斯决策的\\干扰源搜索、定位与清除}}
\author{}
\date{}

\begin{document}

% 第一页
\maketitle

\begin{abstract}
针对测向误差、有限接收距离及未知发射方向下的干扰源搜索清除问题，
本文以“观测更新判断、判断指导行动”为主线，建立几何约束下的贝叶斯决策方法：
用可行区域保留尚未排除的位置，用观测后的概率分布预测反馈，按期望定位范围或预计完成时间选择下一步行动。

针对问题一，将各次示向的误差范围写成半平面约束，通过半平面交构造定位多边形，
证明其直径可由顶点对取得，并建立直径圆覆盖整个区域的充要判据。
我们构造了直径圆能够覆盖与不能覆盖定位区域的两组算例，
说明以区域直径为直径的圆不一定包含整个区域，后续问题中需用最小覆盖圆判断能否保证清除。

针对问题二，设计基于贝叶斯预测的主动选点算法。
首先假定接收半径固定，由首次观测更新位置后验，预测各候选测点出现正常示向、距离过近及无信号的概率；
再分别更新对应可行区域，按反馈概率加权最小覆盖圆半径得到数学期望，最终通过粗网格搜索与局部加密获得推荐测点及近优候选区域。
在接收半径为 $1200\,\mathrm m$ 的两组算例中，推荐点的期望覆盖半径分别为 $23.15\,\mathrm m$ 和 $12.93\,\mathrm m$。
与从首测点侧移 $300\,\mathrm m$ 的选点策略相比，两组算例的期望覆盖半径分别降低约 $68.2\%$ 和 $58.5\%$。

针对问题三，我们将第二问的贝叶斯选点方法扩展到多个干扰源的连续搜索与清除。
每次获得检测或清除结果后，更新各频道存在干扰源的概率及其可能位置，
再比较继续测向、前往清除和搜索未知频道这三类行动。
综合移动距离、操作耗时及可能的反馈，估计各方案完成剩余任务所需的时间，优先执行预计用时较少的下一步行动。
同时利用清除时的停留位置检测其他频道，减少重复移动，并在获得新反馈后重新安排路线，
直至已发现的源全部清除，且确认其余频道不存在遗漏源。

针对问题四，继续将第三问的算法扩展到全向源与定向源混合的情形。
由于未收到信号既可能是距离过远，也可能是位于定向源的发射背面，
算法结合历次检测结果，排除不符合观测的位置、接收半径和发射方向，
再用贝叶斯方法更新其余可能情况的概率，据此预测在哪里检测更可能获得信号，并继续按预计完成时间安排行动。
为避免遗漏定向源，进一步利用多个测点均无信号的结果判断哪些区域已被排除，
设计覆盖整个目标区域的21点检测布局，用于搜索尚未发现的源并确认是否全部清除。

\textbf{关键词：} 贝叶斯决策；半平面交；最小覆盖圆；覆盖搜索；路径规划
\end{abstract}

% 正文开始

\newpage

\section{问题背景与重述}
\subsection{问题背景}
无线电干扰源的定位与清除是无线电频谱管理的重要任务。通过固定监测站、移动监测车或无人机开展初步监测与区域排查后，通常能够确定干扰源的大致分布范围。然而，受环境遮挡和干扰源功率等因素影响，精准定位仍需技术人员携带测向设备进入目标区域，依据多次检测结果逐步缩小搜索范围。为缩短干扰持续时间，并减少人员在危险环境中的作业风险，有必要研究搭载于机器狗的自动定位与清除方法。

测向机通过接收信号获得干扰源相对于检测点的方位信息，但受测量误差影响，单次示向度只能限定目标所在的一个角度范围。不同位置的观测可以进一步收紧定位区域，其效果与检测点的空间分布有关。此外，测向机的有效接收距离有限；对于具有特定辐射方向的干扰源，即使距离较近，也可能无法接收到信号。因此，检测结果既受到目标位置的影响，也与接收范围和辐射方向有关。

机器狗需要在移动、切换频道和停驻检测之间作出安排，并在接近干扰源后完成光学精确定位与清除。这些操作均消耗时间，单纯追求更小的定位区域未必能够缩短整个任务的耗时。因而，需要将\textbf{定位精度与搜索清除效率}结合起来，在确保所有干扰源被清除的前提下，合理选择检测位置并规划后续行动。
\subsection{问题重述}
题目已将干扰源的分布范围限定为半径为 $1800\,\mathrm m$ 的圆域，并给出了测向误差、有效接收距离及各项操作的时间与距离约束。根据所提供的信息，需要建立数学模型解决以下问题。

\textbf{问题一：}已知多个检测点的坐标及同一干扰源在各点的示向度，设计交会定位区域的构造与直径计算方法，并判断是否存在以该区域直径为直径、能够覆盖整个定位区域的圆盘。

\textbf{问题二：}已知某全向干扰源在第一个检测点处的示向度，明确较好定位效果的评价依据，制定第二个检测点的选择策略，并给出相应的候选区域。

\textbf{问题三：}目标区域内有 $10$--$16$ 个全向干扰源，具体数量未知。机器狗从原点出发，初始检测频道为 $1$，要求在\textbf{确保全部清除}的前提下尽量缩短任务总时间。设计自动搜索、定位与清除算法，通过模拟器演练统计清除比例和平均定位清除时间，并完成三次正式测试，报告结果及保存相应日志。

\textbf{问题四：}将目标扩展为全向与定向干扰源混合的情形，总数仍为 $10$--$16$ 个，定向源的数量和辐射方向均未知。在其余条件与问题三相同的情况下，制定适用的检测与清除策略，检验算法的有效性，并按要求完成三次正式测试及结果记录。


\section{问题分析}

四问共同围绕一个过程展开：机器狗依据已有观测判断源的可能状态，选择行动以获取新信息或完成清除，
再用实际反馈更新判断。为使这一过程可计算，本文保留两类信息：
\textbf{几何可行区域}回答源还可能在哪里，\textbf{后验分布}描述这些位置及未知参数的相对可能性。
在此基础上预测下一步反馈，比较行动代价，形成表~\ref{tab:decision-thread} 所示的递进关系。

\begin{table}[htbp]
  \centering
  \caption{从几何定位到贝叶斯行动选择的递进关系}
  \label{tab:decision-thread}
  \small
  \begin{tabularx}{\linewidth}{lXX}
    \toprule
    问题 & 已有信息如何转化 & 为后续行动提供什么 \\
    \midrule
    问题一 & 示向误差界形成定位区域 & 区域尺度与完整覆盖判据 \\
    问题二 & 首次观测形成位置后验，预测第二次反馈 & 期望定位范围较小的测点 \\
    问题三 & 连续反馈更新各频道的存在与位置信息 & 预计剩余时间较少的行动 \\
    问题四 & 将类型、半径与朝向纳入联合更新 & 定向条件下的搜索与清除安排 \\
    \bottomrule
  \end{tabularx}
\end{table}

概率更新服务于选点和排序，完成判定仍由几何覆盖与实际反馈支持。
因此，本文的贝叶斯决策主线建立在可行性约束之上，几何基础与概率评价相互衔接。

\subsection{问题一}

后续概率更新与行动评价首先需要明确位置约束和定位指标。
本问将示向误差转化为楔形交集，利用半平面交构造定位多边形，
再由顶点对求直径、由覆盖判据判断直径圆是否包含整个区域。
这一部分仅依赖有界误差和几何关系，提供后续位置分布的取值范围及最小覆盖圆指标，
最后用两组各含10个检测点的构造算例展示不同覆盖结果。


\subsection{问题二}
有了区域的计算方法，下一步是主动选择能够改善定位的观测位置。
在给定接收半径下，由首次示向与位置先验得到位置后验，预测各候选测点可能出现的正常示向、距离过近与无信号反馈。
每种反馈对应问题一的几何区域更新，再以反馈概率加权最小覆盖圆半径。
由此将“哪个位置更值得检测”转化为期望定位范围的比较，得到推荐测点及近优候选区域。

\subsection{问题三}

多源任务需要连续决定先处理哪个频道、何时补测以及何时清除。
本问沿用第二问的后验更新与反馈预测，将行动评价由定位范围扩展为预计完成时间，
并用每次实际反馈更新频道状态、接收半径约束和位置分布。
已发现源与未知频道的覆盖任务共同进入路线规划，清除停留点也可承担检测。
这一滚动过程持续到成功清除记录与频道排除证据共同支持任务完成。

\subsection{问题四}

定向源使无信号反馈同时具有距离和朝向两种解释，需要扩展用于预测反馈的未知状态。
本问将源类型、半径与朝向共同纳入贝叶斯更新，以同一组参数解释该源的全部历史记录，
再沿用第三问的时间评价选择行动。
为保持搜索完整性，同时建立多个无信号测点的联合排除判据并构造全域覆盖布局，
最后用随机地图与压力地图考察混合发射模式下的完整任务结果。


\section{模型假设}
\label{sec:assumptions}

在题设场景下，我们作出如下假设与计算约定。
\begin{enumerate}
  \item \textbf{任务期间外部环境保持稳定。}干扰源位置不随时间变化，环境对信号接收的影响由题设的有效接收范围和测角误差描述，不另行考虑突发遮挡、设备故障等因素。
  \item \textbf{检测点坐标可准确获得。}忽略机器狗自身定位误差及运动控制误差，忽略地形高度变化、加减速和转向等造成的额外耗时。
  \item \textbf{采用均匀测角误差模型。}对同一干扰源，假设示向度相对真实方位角的误差在 $[-1^\circ,1^\circ]$ 内均匀分布，并认为不同检测位置的误差相互独立。
  \item \textbf{采用面积均匀的位置先验。}在尚未获得某干扰源的观测信息时，假设其出现在目标圆域内等面积区域的概率相同；获得检测反馈后，再更新其位置分布。
\end{enumerate}

\section{符号说明}
全文沿用表~\ref{tab:symbol} 的符号。下标 $i=1,\ldots,n$ 表示检测点，
$j,k=1,\ldots,m$ 表示多边形顶点；坐标分量用下标 $x,y$ 区分。
角度以正东方向为零点、逆时针为正，计算时使用弧度。
\begin{table}[htbp]
  \centering
  \caption{主要符号及约定}
  \label{tab:symbol}
  \label{tab:q1-symbols}
  \begin{tabularx}{\linewidth}{lX}
    \toprule
    符号 & 含义 \\
    \midrule
    $n,\,m$ & 检测点数、定位多边形的顶点数 \\
    $\boldsymbol p_i=(p_{i,x},p_{i,y})^{\mathsf T}$ & 第 $i$ 个检测点的平面坐标（$\mathrm m$） \\
    $\theta_i,\,\varepsilon$ & 第 $i$ 个检测点的示向度、误差上界，$\varepsilon=\pi/180$ \\
    $\boldsymbol n_i^-,\,\boldsymbol n_i^+$ & 第 $i$ 个楔形的两条边界的向内单位法向量；上标 $-,+$ 对应 $\theta_i-\varepsilon,\theta_i+\varepsilon$ \\
    $W_i,\,P$ & 第 $i$ 个楔形区域、全部楔形的交集 \\
    $\boldsymbol x,\,\boldsymbol s$ & 待判断的位置、模拟数据中的真实源位置（$\mathrm m$） \\
    $\boldsymbol v_j$ & 按逆时针排序的第 $j$ 个多边形顶点（$\mathrm m$） \\
    $D;\ \boldsymbol a,\boldsymbol b$ & 区域直径及取得直径的一对顶点（$\mathrm m$） \\
    $\boldsymbol c_D$ & 以 $\boldsymbol a\boldsymbol b$ 为直径的圆心（$\mathrm m$） \\
    $R_*,\,\boldsymbol c_*$ & 最小覆盖圆的半径和圆心（$\mathrm m$） \\
    \bottomrule
  \end{tabularx}
\end{table}

\FloatBarrier
\section{模型建立与求解}
\subsection{问题一模型建立}
\label{sec:q1}
% 按整合修改意见修订问题一；单位与符号统一、图像素材核对仍留待后续。

本问先建立观测到位置约束的几何映射。所得区域为后续贝叶斯更新限定可能位置，
其最小覆盖圆则用于评价定位范围和判断能否保证清除。

\subsubsection{定位区域}
\label{sec:q1-wedge}




本问的定位多边形指全部示向约束形成的楔形交集，不叠加距离或圆形活动范围的裁剪；
若加入这些约束，边界可能含有圆弧，需另行处理。
示向度误差为 $\pm1^\circ$，因此干扰源应在 $\theta_i-\varepsilon$ 与
$\theta_i+\varepsilon$ 两条射线夹成的楔形内。直接比较角度需要处理跨越 $0^\circ$ 的情况，
这里改用\textbf{法向量内积判断点位于边界的哪一侧}。
将下侧边界的方向向量逆时针旋转 $90^\circ$，上侧边界的方向向量顺时针旋转 $90^\circ$，得到
\[
  \begin{aligned}
    \boldsymbol n_i^-&=\bigl(-\sin(\theta_i-\varepsilon),\ \cos(\theta_i-\varepsilon)\bigr)^{\mathsf T},\\
    \boldsymbol n_i^+&=\bigl(\sin(\theta_i+\varepsilon),\ -\cos(\theta_i+\varepsilon)\bigr)^{\mathsf T}.
  \end{aligned}
\]
这两个单位法向量分别朝向各自边界包含楔形的那一侧，于是
\begin{equation}
  W_i=\left\{\boldsymbol x\in\mathbb R^2:
  (\boldsymbol n_i^-)^{\mathsf T}(\boldsymbol x-\boldsymbol p_i)\geq0,\quad
  (\boldsymbol n_i^+)^{\mathsf T}(\boldsymbol x-\boldsymbol p_i)\geq0\right\}.
  \label{eq:q1-wedge}
\end{equation}


式~\eqref{eq:q1-wedge} 中，$\boldsymbol x-\boldsymbol p_i$ 是从检测点指向待判断位置的向量，
它与单位法向量的内积，就是沿该法向量的投影，也就是到边界直线的有向距离。
\textbf{内积为正时，点在朝向楔形内部的一侧；为零时在边界上；为负时在外侧。}
两项必须同时非负，才能说明该点位于两条射线之间。
例如，沿示向方向前进距离 $t>0$，两项内积都为 $t\sin\varepsilon>0$；
若反向移动，则两项均为负。因此，该判据也排除了检测点后方的区域。




将每次检测的两个约束合并，可得
\begin{equation}
  P=\bigcap_{i=1}^{n}W_i=\{\boldsymbol x:H\boldsymbol x\leq\boldsymbol g\}.
  \label{eq:q1-intersection}
\end{equation}
矩阵 $H$ 的第 $2i-1,2i$ 行分别为 $-(\boldsymbol n_i^-)^{\mathsf T}$、
$-(\boldsymbol n_i^+)^{\mathsf T}$，$\boldsymbol g$ 的对应分量为这两行与 $\boldsymbol p_i$ 的乘积。
对任意 $\boldsymbol x,\boldsymbol y\in P$ 及 $\lambda\in[0,1]$，有
\[
  H\bigl(\lambda\boldsymbol x+(1-\lambda)\boldsymbol y\bigr)
  \leq\lambda\boldsymbol g+(1-\lambda)\boldsymbol g=\boldsymbol g.
\]
因此两点间的整条线段仍在 $P$ 内，故 $P$ 为凸集，这也是交集保凸性的直接体现。
下文先讨论 $P$ 非空、有界且内部非空的情形，此时它是凸多边形。
由有界多面体的顶点表示定理\cite{boyd2004convex}，
$P=\operatorname{conv}(V)$，其中 $V=\{\boldsymbol v_1,\ldots,\boldsymbol v_m\}$ 为全部顶点集合，
$\operatorname{conv}(V)$ 表示顶点所有凸组合构成的集合。


\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.94\linewidth]{data/q1_wedge_intersection/q1_wedge_intersection.png}
  \caption{楔形交会与定位区域（误差角放大示意）}
  \label{fig:q1-wedges}
\end{figure}




\label{sec:q1-filter}
\label{sec:q1-polygon}
求取多边形时，最直接的办法是\textbf{枚举边界交点}。
将 $2n$ 条边界射线所在的直线作为候选边界，仅对不平行的直线求交，
再用式~\eqref{eq:q1-intersection} 的全部约束筛选；平行或重合直线不作为唯一交点的来源。
保留点去重后求凸包，得到按逆时针排列的顶点，无须额外判断射线参数。
记保留点集为 $S_{\mathrm f}$。每个多边形顶点至少位于两条不平行的活跃边界上
（即对应不等式在该点取等号），否则该点可沿边界方向向两侧移动而仍保持可行，不能成为顶点。
因此枚举不会遗漏顶点，且筛选保证 $V\subseteq S_{\mathrm f}\subseteq P$，从而
\[
  P=\operatorname{conv}(V)\subseteq\operatorname{conv}(S_{\mathrm f})\subseteq P.
\]
这证明所得凸包恰好是完整定位区域。候选交点为 $O(n^2)$ 个，每点检查 $2n$ 个约束，
连同去重和凸包计算，区域构造复杂度为 $O(n^3)$。

另一种办法是直接求\textbf{半平面交}\cite{deberg2008geometry}。先将边界统一为可行半平面位于左侧的有向直线，
按方向角排序，同向平行边界仅保留较严格者；再用双端队列维护有效边界。
加入新约束时，删除队首或队尾使相邻交点落在新半平面外侧的边界，最后检查首尾闭合。
排序耗时 $O(n\log n)$，每条边界至多入队、出队一次，故区域构造复杂度为 $O(n\log n)$。
交点枚举便于逐项检查约束，半平面交降低构造阶段的渐近复杂度；两者基于同一模型，结果比较用于核对实现。

算法还需识别区域状态：空集表示约束不相容，非空无界区域没有有限直径；
退化为点或有界线段时，直径分别为零或线段长度。
不能仅凭没有找到交点判断为空，也不能以人为边框将无界区域当成有界多边形。


\subsubsection{区域直径}
\label{sec:q1-diameter}




区域直径描述仍与观测相容的任意两个位置之间的最大距离。
由于 $P$ 是闭且有界的非空多边形，该最大值能够取得。

\noindent\textbf{命题1（顶点直径）}\quad 定位多边形的直径可由一对顶点取得，即
\begin{equation}
  D=\max_{\boldsymbol x,\boldsymbol y\in P}\|\boldsymbol x-\boldsymbol y\|_2
   =\max_{1\leq j,k\leq m}\|\boldsymbol v_j-\boldsymbol v_k\|_2.
  \label{eq:q1-vertex-diameter}
\end{equation}
\noindent\textbf{证明：}由 $P=\operatorname{conv}(V)$，任意 $\boldsymbol x,\boldsymbol y\in P$ 均可写成顶点的凸组合
$\boldsymbol x=\sum_j\alpha_j\boldsymbol v_j$、$\boldsymbol y=\sum_k\beta_k\boldsymbol v_k$，
其中权重非负且各自的和为 $1$。由三角不等式，
\[
  \|\boldsymbol x-\boldsymbol y\|_2
  =\left\|\sum_{j,k}\alpha_j\beta_k(\boldsymbol v_j-\boldsymbol v_k)\right\|_2
  \leq\sum_{j,k}\alpha_j\beta_k\|\boldsymbol v_j-\boldsymbol v_k\|_2
  \leq\max_{j,k}\|\boldsymbol v_j-\boldsymbol v_k\|_2.
\]
又因顶点本身属于 $P$，等号可以由一对最远顶点取得。
因此，无须对区域内部进行空间采样，枚举顶点对即可在 $O(m^2)$ 时间内求出 $D$，
并记录对应端点 $\boldsymbol a,\boldsymbol b$。该化简消除了空间网格近似，但计算仍受浮点精度影响。


\subsubsection{圆的覆盖}
\label{sec:q1-cover-counterexample}
\label{sec:q1-cover-condition}




下文所称覆盖圆均指包含内部及边界的闭圆盘，记
$B(\boldsymbol c,r)=\{\boldsymbol x:\|\boldsymbol x-\boldsymbol c\|_2\leq r\}$。
取直径端点 $\boldsymbol a,\boldsymbol b$，记其中点为 $\boldsymbol c_D=(\boldsymbol a+\boldsymbol b)/2$。
若半径为 $D/2$、圆心为 $\boldsymbol o$ 的圆盘能覆盖 $P$，则必包含这两个端点，故
\[
  D=\|\boldsymbol a-\boldsymbol b\|_2
  \leq\|\boldsymbol a-\boldsymbol o\|_2+\|\boldsymbol o-\boldsymbol b\|_2\leq D.
\]
两处均须取等，因而 $\boldsymbol o$ 在线段 $\boldsymbol a\boldsymbol b$ 上，
且到两端点的距离均为 $D/2$，即 $\boldsymbol o=\boldsymbol c_D$。
所以满足要求的覆盖圆若存在，其圆心只能是该中点。

然而，中点圆不一定覆盖整个区域。例如，边长为 $D>0$ 的等边三角形，
任取一条边，其对顶点到该边中点的距离为 $\sqrt{3}D/2>D/2$。
结合圆心唯一性可知，不存在半径为 $D/2$ 的圆盘覆盖该三角形。
图~\ref{fig:q1-cover-cases} 给出了三个检测点的楔形交形成三角形时，不能覆盖与可以覆盖的两种情形，
各楔形张角均为 $2^\circ$。


\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.94\linewidth]{data/q1_cover_cases/q1_cover_cases.png}
  \caption{直径圆不能覆盖与可以覆盖的情形}
  \label{fig:q1-cover-cases}
\end{figure}




\noindent\textbf{命题2（直径圆覆盖判据）}\quad 对定位多边形 $P$，有
\begin{equation}
  \exists\,\boldsymbol o:\ P\subseteq B(\boldsymbol o,D/2)
  \quad\Longleftrightarrow\quad
  \max_{1\leq j\leq m}\|\boldsymbol v_j-\boldsymbol c_D\|_2\leq D/2.
  \label{eq:q1-cover-test}
\end{equation}
\noindent\textbf{证明：}必要性由圆心唯一性得到：若存在覆盖圆，其圆心必为 $\boldsymbol c_D$，
全部顶点均满足距离条件。反之，若全部顶点都在该圆盘内，由圆盘的凸性及
$P=\operatorname{conv}(V)$，整个多边形也在其中，充分性成立。

因此无须搜索圆心，得到任一对直径端点后，用 $O(m)$ 时间检查全部顶点即可完成判定。
点和有界线段分别可由零半径圆盘和以该线段为直径的圆盘覆盖。
采用半平面交、顶点对枚举和上述判据时，区域构造、直径计算与覆盖判断的总复杂度为
$O(n\log n+m^2)$；此处不包括下面作为补充的最小覆盖圆计算。

为描述不能由直径圆覆盖时所需的尺度，定义最小覆盖圆半径
\begin{equation}
  R_*=\min_{\boldsymbol c\in\mathbb R^2}\max_j\|\boldsymbol v_j-\boldsymbol c\|_2
  \geq D/2.
  \label{eq:q1-minimum-circle}
\end{equation}
下界由任一覆盖圆必须包含直径两端点得到，等号成立当且仅当式~\eqref{eq:q1-cover-test} 成立。
有限点集的最小覆盖圆由至多三个边界点确定：非退化时由两点作为直径端点，
或由三个不共线点确定外接圆。因此算例枚举不同两点的直径圆及不共线三点的外接圆，
在包含全部顶点的候选圆中取半径最小者；单点情形取零半径。


\subsubsection{数值算例与结果可视化}
\label{sec:q1-experiment}

构造两组各含 $10$ 个检测点、满足 $\pm1^\circ$ 测向误差约束的示向数据。
根据上述半平面交算法，能够得到表~\ref{tab:q1-experiment} 及图~\ref{fig:q1-experiment} 所示的定位结果。
检测点坐标与示向度见支撑材料。

\begin{table}[htbp]
  \centering
  \caption{两组算例的定位结果与直径圆覆盖关系}
  \label{tab:q1-experiment}
  \begin{tabular}{lcc}
    \toprule
    指标 & 算例1 & 算例2 \\
    \midrule
    顶点数 & 6 & 6 \\
    区域直径 $D$（$\mathrm m$） & 20.00 & 20.02 \\
    直径圆半径 $D/2$（$\mathrm m$） & 10.00 & 10.01 \\
    最大覆盖超差 $\delta$（$\mathrm m$） & 0.00 & 5.52 \\
    直径圆能否覆盖 & 能 & 不能 \\
    \bottomrule
  \end{tabular}
  \par\smallskip
  \parbox{0.94\linewidth}{\footnotesize 注：$\delta=\max_j\|\boldsymbol v_j-\boldsymbol c_D\|_2-D/2$。
  数值保留两位小数，覆盖判定使用未舍入结果，计算容差为 $10^{-8}\,\mathrm m$。}
\end{table}

图~\ref{fig:q1-experiment} 左侧展示检测点的整体布置，右侧放大相应定位区域。
其中，$AB$ 为直径线段，$O$ 为其中点。算例1的全部顶点均位于直径圆内或圆周上；
算例2中，顶点 $C$ 到圆心的距离约为 $15.53\,\mathrm m$，超出圆半径约 $5.52\,\mathrm m$，
因而不能覆盖。两组区域的直径接近，但覆盖结果不同，展示了区域形状对覆盖关系的影响。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.94\linewidth]{data/q1_two_cases/q1_two_cases.png}
  \caption{两组算例的检测点布置、定位区域与直径圆}
  \label{fig:q1-experiment}
\end{figure}

% 数值与输入：data/q1_two_cases/results.json、case1_observations.csv、case2_observations.csv。
% 绘图与主算法：data/q1_two_cases/generate.py、geometry.py。
\label{sec:q1-end}


\subsection{问题二模型建立与求解}
\label{sec:q2}
% 本节沿用固定R0模型；表述与基线对照按第二问审查意见前五项整理。

\subsubsection{定位指标与选点准则}
\label{sec:q2-effect}

问题一给出了“获得示向后如何确定区域”的方法，本问进一步决定“下一次应到哪里观测”。
第二测点既影响交会几何，也影响能否接收到信号，因而需要先由已有观测更新位置分布，
再预测各候选点的可能反馈，比较\textbf{反馈发生概率与区域收缩效果共同决定的期望定位范围}。
下面依次给出评价准则、首次观测后的后验分布及反馈预测方法。

沿用第一问的\textbf{最小覆盖圆半径 $R_*$}评价定位范围。相比面积，该指标能够反映狭长区域
在最不确定方向上的尺度；以最小覆盖圆圆心作为位置估计时，$R_*$ 即全部可行位置对应的
最坏情况误差。区域直径保留为辅助指标。

对候选点 $\boldsymbol q$，记可能反馈的集合为 $\mathcal Y$，其中包括正常示向的角度档、
“距离过近”和“无信号”。记反馈 $y$ 的预测概率为 $\pi_y(\boldsymbol q)$，
反馈后的完整可行区域为 $P_2(\boldsymbol q,y)$，则选点评价函数为
\begin{equation}
  J(\boldsymbol q)=\sum_{y\in\mathcal Y}
    \pi_y(\boldsymbol q)R_*\!\left(P_2(\boldsymbol q,y)\right).
  \label{eq:q2-objective}
\end{equation}
概率用于综合尚未发生的观测结果，各反馈下的覆盖半径则描述相应的几何定位范围。
因此，$J$ 能够衡量给定概率模型下的平均定位效果。

在目标圆域内建立候选网格 $\mathcal G$，定义推荐点与近优候选集合为
\begin{equation}
  \begin{aligned}
    \boldsymbol q_*&\in\underset{\boldsymbol q\in\mathcal G}{\operatorname{argmin}}\,J(\boldsymbol q),\\
    \mathcal C_\eta&=\left\{\boldsymbol q\in\mathcal G:
    J(\boldsymbol q)\leq(1+\eta)J(\boldsymbol q_*)\right\}.
  \end{aligned}
  \label{eq:q2-candidate-set}
\end{equation}
下文取 $\eta=0.1$ 展示 $10\%$ 近优区域，表示允许期望覆盖半径相对推荐点增加不超过 $10\%$。
这一阈值是候选区域的选取约定。下面先建立首次观测后的位置信息，再给出反馈预测与数值计算方法。

\subsubsection{首次观测后的位置信息}
\label{sec:q2-bayes}

首先，由于\textbf{同一目标干扰源的发射功率及测向机的灵敏度确定}，本问针对同一目标干扰源进行的两次检测，将接收半径
$R_0\in[1000,1500]\,\mathrm m$ 当成\textbf{固定参数}进行处理，在两次检测及候选测点的比较中保持不变。
以下，在给定 $R_0$ 的条件下，固定第一检测点 $\boldsymbol p_1$ 及其正常示向度 $\theta_1$，
确定第二检测点。记圆形目标区域为 $\Omega$，则第一次观测后的可行区域为
\begin{equation}
  P_1=\Omega\cap W_1\cap
  \{\boldsymbol x:5<\|\boldsymbol x-\boldsymbol p_1\|_2\leq R_0\}.
  \label{eq:q2-first-region}
\end{equation}
式中三个约束分别\textbf{对应目标分布范围}、\textbf{示向度误差范围}和\textbf{正常测向的距离条件}。
其中，$W_1$ 由第一次示向度及其两侧各 $\varepsilon=1^\circ$ 的误差确定（楔形）；
目标圆域与距离条件进一步裁剪该楔形，所得 $P_1$ 构成第二次选点的初始区域。

依据第~\ref{sec:assumptions} 节的位置先验与测角误差假设，目标在 $\Omega$ 内按单位面积均匀分布，
首次测角误差在 $[-\varepsilon,\varepsilon]$ 内均匀分布。由此，各可行位置产生第一次示向度的
似然密度均为 $1/(2\varepsilon)$，经贝叶斯更新\cite{gelman2013bayesian}后，位置分布在 $P_1$ 内仍保持均匀。
因此，$P_1$ 内任一子区域的概率等于其面积占比。这一关系为位置离散后的概率赋值提供了依据。

\subsubsection{反馈预测与数值计算}
\label{sec:q2-numerics}

为计算式~\eqref{eq:q2-objective}，将位置积分、反馈预测和区域更新结合起来。
位置积分采用扇环单元，尚未获得的示向度采用角度分档；二者分别用于近似位置分布和反馈分布。
计算时，将圆形区域划分为小扇环，并保留与 $P_1$ 相交的部分。
用 $\ell$ 表示位置单元编号，$E_\ell$ 表示裁剪后的单元，
$\boldsymbol z_\ell$ 为其中的代表点，则该单元的概率权重为
\[
  w_\ell=\frac{\operatorname{Area}(E_\ell)}{\operatorname{Area}(P_1)},
  \qquad \sum_\ell w_\ell=1.
\]
\textbf{单元权重由实际面积确定}：只有等面积单元才具有相同概率，
等径向间距、等角度间距的扇环单元\textbf{仍需按面积加权}。
对边界单元，使用其与 $P_1$ 的交集面积；代表点用于近似单元内的积分，
在测角或距离边界附近进一步细分，以减小离散误差。

\begin{figure}[htbp]
  \centering
  \begin{minipage}[b]{0.48\linewidth}
    \centering
    \parbox[c][5.4cm][c]{\linewidth}{\centering
      \includegraphics[width=\linewidth,height=5.2cm,keepaspectratio]{data/q2_initial_region_v1/q2_initial_region_v1.png}}
    \par\smallskip
    {\small (a) 初始可行区域}
  \end{minipage}\hfill
  \begin{minipage}[b]{0.48\linewidth}
    \centering
    \parbox[c][5.4cm][c]{\linewidth}{\centering
      \includegraphics[width=\linewidth,height=5.2cm,keepaspectratio]{data/q2_annular_grid_v3/q2_annular_grid_v3.png}}
    \par\smallskip
    {\small (b) 扇环离散示意}
  \end{minipage}
  \caption{初始可行区域与位置离散（误差角及近距离排除区域放大示意）}
  \label{fig:q2-discretization}
\end{figure}

对候选第二检测点 $\boldsymbol q$，记代表位置 $\boldsymbol z_\ell$ 相对该测点的
距离和真实方位分别为 $d_\ell(\boldsymbol q)$、$\varphi_\ell(\boldsymbol q)$。
根据测向机的工作条件，第二次检测包含三类反馈：距离不超过 $5\,\mathrm m$ 时返回
“距离过近”，超过 $R_0$ 时返回“无信号”，其余情形返回带有误差的示向度。
三类反馈共同构成选点评价所需的完整观测空间，不能只考虑成功返回示向度的情形。

实际获得第二次示向度后，直接以该读数为中心构造\textbf{总张角为 $2^\circ$ 的楔形}，
与已有可行区域及距离约束求交即可。
但选点时读数尚未确定，为计算期望，将 $[0,2\pi)$ 划分为互不重叠的角度档 $B_h$。
在均匀误差假设下，第 $\ell$ 个位置单元产生第 $h$ 档反馈的概率，
可由代表点的读数范围与该档的重叠角长占比近似计算：
\begin{equation}
  L_{\ell h}(\boldsymbol q)\approx
  \operatorname{Bool}\!\left(5<d_\ell(\boldsymbol q)\leq R_0\right)
  \frac{\left|B_h\cap
  [\varphi_\ell(\boldsymbol q)-\varepsilon,\,
   \varphi_\ell(\boldsymbol q)+\varepsilon]\right|_{\mathrm{circ}}}{2\varepsilon}.
  \label{eq:q2-likelihood}
\end{equation}
其中，$\operatorname{Bool}(\cdot)$ 为布尔型函数，在括号内条件成立时取 $1$，否则取 $0$；
$|\cdot|_{\mathrm{circ}}$ 为圆周角长。“距离过近”和“无信号”的概率由相应距离条件确定，
三类反馈的概率之和为 $1$。\textbf{角度分档仅用于选点前的期望计算，不改变实测后的两度楔形。}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.6\linewidth]{data/q2_reception_clipping_v4/q2_reception_clipping_v4.png}
  \caption{不同第二示向度下的可行区域更新（误差角放大示意）}
  \label{fig:q2-prediction}
\end{figure}

图~\ref{fig:q2-prediction} 中，两组示向边界表示同一候选测点处的两种可能反馈，
分别与初始区域求交，而非同时施加约束；图示仅说明区域更新，反馈概率由式~\eqref{eq:q2-likelihood} 计算。

用 $\mathcal Y$ 表示包含各角度档、“距离过近”和“无信号”的反馈集合。
根据全概率公式和贝叶斯公式，反馈 $y$ 的预测概率与反馈后的单元权重分别为
\begin{equation}
  \pi_y(\boldsymbol q)=\sum_\ell w_\ell L_{\ell y}(\boldsymbol q),\qquad
  w_{\ell\mid y}(\boldsymbol q)
  =\frac{w_\ell L_{\ell y}(\boldsymbol q)}{\pi_y(\boldsymbol q)}.
  \label{eq:q2-bayes}
\end{equation}
前式对所有可能位置加权求和，得到选点时所需的反馈分布；
后式在给定反馈后更新位置概率，排除不相容的位置，并调整其余单元的相对权重。
将这些预测概率代入式~\eqref{eq:q2-objective}，便完成从位置后验到测点评价的转换；
执行检测后，仅用实际获得的反馈更新后验。这也是问题三、四逐步选择行动的基础。
其中，后验更新仅对 $\pi_y(\boldsymbol q)>0$ 的反馈进行，
预测分布满足 $\sum_{y\in\mathcal Y}\pi_y(\boldsymbol q)=1$。

与概率更新相对应，记反馈后的\textbf{完整可行区域}为 $P_2(\boldsymbol q,y)$。
对于具体示向度，该区域由 $P_1$ 与第二楔形及正常测向距离范围相交得到。
在角度分档计算中，一档包含多个可能的示向度，应取这些示向度对应楔形的并集。
对于宽度为 $\Delta\theta$ 的窄角度档，该并集等价于以档中心为方向、
半张角为 $\varepsilon+\Delta\theta/2$ 的楔形。
其中增加的 $\Delta\theta/2$ 用于保留分档所包含的角度不确定性，并随档宽减小而趋于零。

对于“距离过近”，$P_2$ 为 $P_1$ 与以 $\boldsymbol q$ 为圆心、半径 $5\,\mathrm m$
的圆盘的交集，其最小覆盖圆半径不超过 $5\,\mathrm m$；
对于“无信号”，$P_2$ 为 $P_1$ 中距离 $\boldsymbol q$ 超过 $R_0$ 的部分。
两类反馈均通过\textbf{距离约束}更新可行区域，其定位效果同样由更新后区域的覆盖半径评价。

将得到的反馈概率与区域覆盖半径代入式~\eqref{eq:q2-objective}，即可计算各候选点的 $J$，
并按式~\eqref{eq:q2-candidate-set} 确定推荐点与候选区域。

按题设，同一位置重复检测的读数不变，故第一检测点可以单独取
$J(\boldsymbol p_1)=R_*(P_1)$。

下文位置积分采用 $80\times16$ 个扇环单元，示向度档宽为 $0.125^\circ$。
候选网格间距为 $60\,\mathrm m$，在粗网格最佳点附近的 $\pm90\,\mathrm m$ 范围内加密至
$15\,\mathrm m$；对称情形同时考察镜像邻域。推荐坐标与目标值均对应这一离散计算设置。

\subsubsection{结果可视化与策略比较}
\label{sec:q2-experiment}

为考察初始几何关系对第二检测点选择的影响，我们设置了对称与不对称两组情形。
目标圆域半径均取 $1800\,\mathrm m$，接收半径固定为 $R_0=1200\,\mathrm m$，
测角误差半宽为 $1^\circ$。对称情形取 $\boldsymbol p_1=(0,0)\,\mathrm m$、
初次示向度 $0^\circ$；不对称情形取 $\boldsymbol p_1=(1200,400)\,\mathrm m$、
初次示向度 $65^\circ$。下面先展示推荐区域，再用一个构造情形呈现交会结果，
最后比较不同选点策略的期望定位效果。

\textbf{推荐测点与候选区域。}图~\ref{fig:q2-experiment} 展示两组情形的期望覆盖半径。
两图共用对数色标，颜色由紫色经蓝绿色趋向黄色时，$J$ 逐渐减小。
星标为网格搜索得到的推荐点，虚线为式~\eqref{eq:q2-candidate-set} 中
$\eta=0.1$ 所对应阈值的网格插值等值线，用于展示近优区域。
颜色块对应实际网格计算值，虚线用于可视化；推荐点不代表连续区域内的严格全局最优解。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{data/q2_strategy_heatmaps/q2_strategy_heatmaps.png}
  \caption{两组初始条件下的推荐测点及近优候选区域（共用对数色标）}
  \label{fig:q2-experiment}
\end{figure}

对称情形下，目标圆域、首测点及首次楔形均关于 $x$ 轴对称，位置先验和误差模型
也保持这一对称性，故上下镜像测点的 $J$ 相同，推荐点为 $(840,\pm495)\,\mathrm m$。
不对称情形中，目标圆域的裁剪打破了关于首示向线的对称性，
推荐点变为 $(1140,945)\,\mathrm m$，需依据裁剪后的初始区域重新选点。

\textbf{不同选点下的交会结果。}为直观展示第二次观测带来的区域变化，
以沿首示向前进 $300\,\mathrm m$、朝其左侧垂直方向移动 $300\,\mathrm m$
作为两种简单选点策略，与推荐点比较。
在对称情形中构造源位置 $\boldsymbol s=(900,0)\,\mathrm m$，首次测角误差取 $0^\circ$，
三种第二测点的测角误差均取 $+0.35^\circ$，均返回正常示向。
根据上述区域更新算法，得到图~\ref{fig:q2-intersection-comparison} 所示的交会区域及最小覆盖圆。

\begin{figure}[!htb]
  \centering
  \includegraphics[width=\linewidth]{data/q2_intersection_comparison/q2_intersection_comparison.png}
  \caption{三种第二测点下的交会结果：上排为整体位置关系，下排为虚框内的局部放大}
  \label{fig:q2-intersection-comparison}
  \par\smallskip
  \parbox{0.96\linewidth}{\footnotesize 注：各列的整体视图采用相同坐标范围，局部视图也采用相同坐标范围。
  左下图的覆盖圆超出局部窗口，其完整形状见左上图。该图展示固定构造情形的单次结果，图中半径不是期望值 $J$。}
\end{figure}

\begin{samepage}
这一构造情形中，初始覆盖半径为 $597.59\,\mathrm m$。
沿示向前进后，两次楔形接近同向，交会区域仍然狭长，覆盖半径为 $447.59\,\mathrm m$；
侧向移动后，第二楔形从不同方向切割初始区域，覆盖半径降至 $105.95\,\mathrm m$；
在推荐点观测后，覆盖半径为 $19.11\,\mathrm m$。
这些结果用于展示区域收缩的几何过程，策略的平均效果仍由各类反馈加权后的 $J$ 评价。
\par
\end{samepage}

\textbf{期望定位效果比较。}表~\ref{tab:q2-baselines} 将测点坐标、期望覆盖半径和无信号概率
集中列出，两组初始覆盖半径分别为 $597.59\,\mathrm m$ 和 $340.22\,\mathrm m$。
所有期望值均采用前述相同数值参数计算。

\begin{table}[!htb]
  \centering
  \caption{推荐测点与简单选点策略的期望定位效果}
  \label{tab:q2-recommendations}
  \label{tab:q2-baselines}
  \small
  \setlength{\tabcolsep}{5pt}
  \renewcommand{\arraystretch}{1.15}
  \begin{tabular}{llcrr}
    \toprule
    情形 & 选点策略 & 测点坐标/m & $J$/m & 无信号概率/\% \\
    \midrule
    对称 & 沿示向前进 & $(300,0)$ & 409.03 & 0.00 \\
         & 侧向移动 & $(0,300)$ & 72.74 & 6.25 \\
         & 推荐点 & $(840,\pm495)$ & 23.15 & 0.00 \\
    \midrule
    不对称 & 沿示向前进 & $(1326.79,671.89)$ & 157.36 & 0.00 \\
           & 侧向移动 & $(928.11,526.79)$ & 31.15 & 0.00 \\
           & 推荐点 & $(1140,945)$ & 12.93 & 0.00 \\
    \bottomrule
  \end{tabular}
  \par\smallskip
  \parbox{0.96\linewidth}{\footnotesize 注：两种简单策略均相对首测点移动 $300\,\mathrm m$；
  $\pm$ 表示一对镜像推荐点。坐标按显示精度取舍，计算使用未舍入坐标。
  无信号概率为位置求积近似值。本表比较定位范围，各策略移动距离不同，未计移动时间。}
\end{table}
% 期望指标沿用 src/Q2/results/paper_baselines/results.json；构造交会结果见 data/q2_intersection_comparison/。

两组情形中，侧移策略的 $J$ 均小于沿示向前进策略；进一步选择前方侧向的推荐点后，
相比侧移 $300\,\mathrm m$，两组 $J$ 分别降低约 $68.2\%$ 和 $58.5\%$。
这一比较综合了可能的第二次反馈，与图~\ref{fig:q2-intersection-comparison} 的单次交会结果相互补充。

选点还受到接收范围的制约。例如，对称情形的侧移测点有约 $6.25\%$ 的无信号概率，
原因是初始区域远端的一部分位置距该测点超过 $R_0$。推荐点则兼顾交会几何与接收条件，
因此应结合整个初始区域比较各种反馈，而不能只依据某个假定源位置的交会角选点。
\FloatBarrier


\subsubsection{给定接收半径下的尺度变化}
\label{sec:q2-scale}

为说明给定接收半径如何改变推荐点和定位范围的尺度，在对称情形下固定
$\boldsymbol p_1=(0,0)\,\mathrm m$、初次示向度 $0^\circ$
及测角误差半宽 $1^\circ$，在题设允许的 $[1000,1500]\,\mathrm m$ 范围内，
以 $100\,\mathrm m$ 为间隔选取六组接收半径 $R_0$，比较第二检测点的选择结果。
目标圆域半径仍为 $1800\,\mathrm m$，每组实验均按相应的 $R_0$ 更新初始可行区域，
并按上述数值设置重新搜索第二检测点。利用镜像对称性，仅搜索上半圆域，
并核验对应镜像点的期望值一致。

\begin{table}[htbp]
  \centering
  \caption{给定接收半径下推荐点与期望定位范围的尺度变化}
  \label{tab:q2-r0}
  \small
  \setlength{\tabcolsep}{7pt}
  \renewcommand{\arraystretch}{1.25}
  \begin{tabular}{ccrrr}
    \toprule
    \shortstack{接收半径\\$R_0$/m} &
    \shortstack{推荐第二检测点\\$(x_2,y_2)$/m} &
    \shortstack{初始半径\\$R_*(P_1)$/m} &
    \shortstack{期望最小覆盖\\圆半径 $J$/m} &
    \shortstack{半径降幅\\/\%} \\
    \midrule
    1000 & $(705,\,\pm 420)$ & 497.6 & 19.3 & 96.1 \\
    1100 & $(765,\,\pm 450)$ & 547.6 & 21.2 & 96.1 \\
    1200 & $(840,\,\pm 495)$ & 597.6 & 23.1 & 96.1 \\
    1300 & $(900,\,\pm 540)$ & 647.6 & 25.1 & 96.1 \\
    1400 & $(975,\,\pm 585)$ & 697.6 & 27.0 & 96.1 \\
    1500 & $(1050,\,\pm 615)$ & 747.6 & 28.9 & 96.1 \\
    \bottomrule
  \end{tabular}
  \par\smallskip
  \parbox{0.94\linewidth}{\footnotesize 注：$\pm$ 表示一对镜像推荐点；$J$ 为各组网格搜索所得的最小期望值，
  半径降幅为 $[1-J/R_*(P_1)]\times100\%$。}
\end{table}

表~\ref{tab:q2-r0} 中，推荐点随 $R_0$ 增大而外移，且归一化坐标
$x_2/R_0$ 约为 $0.692$--$0.705$，$|y_2|/R_0$ 约为 $0.409$--$0.420$，
$J/R_0$ 均约为 $0.0193$，表现出近似的尺度关系。
本组首测点位于圆心且 $R_0<1800\,\mathrm m$，初始区域未受目标圆域外边界裁剪；
除固定的 $5\,\mathrm m$ 近距离排除范围外，几何尺度主要随 $R_0$ 同步变化。
这一关系不宜直接推广到受圆域边界裁剪的其他初始条件。

此外，将位置积分加密至 $160\times32$、示向度档宽减半，并加密圆弧近似后，
在各组原推荐点处重算的期望值变化均不超过 $1.3\%$。这一检查支持表中固定测点目标值的数值稳定性。

\subsection{问题三模型建立与求解}
\label{sec:q3}
% 本节对应 src/Q3 的 bayes-fast：V6策略及等价计算缓存。
% 表述按实际实现整理；正式成绩仍需核对同一版本的原始日志。

问题二通过一次反馈预测选择第二测点，本问将这一过程扩展到多频道的连续行动。
几何区域与后验分布随检测、清除反馈更新，尚未发现的频道还需估计存在概率，因而任务与访问位置不断变化。
为将信息获取与清除进度放在同一尺度上，本问把评价目标从期望定位范围改为预计完成时间。

剩余工作分为\textbf{已发现源的定位清除}和\textbf{未知频道的覆盖搜索}。
每轮根据当前后验预测反馈，比较候选动作及后续路线的费用，只执行评分较小的下一步，再用实际反馈重新决策。
这一框架采用贝叶斯更新处理不确定信息，并用局部时间近似实现行动选择。

\subsubsection{任务目标与决策流程}

沿用目标圆域 $\Omega$，以频道编号 $c$ 区分干扰源。
同一频道至多对应一个源，其接收半径 $R_c\in[1000,1500]\,\mathrm m$ 在任务期间保持不变。
机器狗从原点出发，初始频道为 $1$，每次停驻后检测一个频道。
任务目标是在确认全部干扰源已清除的前提下，尽量减少总虚拟时间：
\begin{equation}
  T=\frac{L}{5}+n_{\mathrm{sw}}+5n_{\mathrm{meas}}
    +5n_{\mathrm{succ}}+3n_{\mathrm{fail}}.
  \label{eq:q3-time}
\end{equation}
其中，$L$ 为总移动路程，$n_{\mathrm{sw}}$、$n_{\mathrm{meas}}$ 分别为实际切频与检测次数，
$n_{\mathrm{succ}}$、$n_{\mathrm{fail}}$ 为成功和失败的清除尝试次数。
成功清除包括 $3\,\mathrm s$ 光学定位与 $2\,\mathrm s$ 激光清除，失败尝试仅计光学定位时间。
各项均以秒计；程序运行时间另行统计，并受模拟器实际剩余时间限制。

只追求较小的定位区域可能造成绕行，只优先清除最近的源也可能增加后续搜索的路程。
因此，选点时既要计入眼前的移动和操作，也要估计执行后还剩多少工作。
图~\ref{fig:q3-flow} 给出这一过程：原点扫描获得初始信息后，持续更新频道状态、比较动作并执行，
直到清除反馈和覆盖记录共同支持任务完成。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{data/q3_bayes_flowchart_bw_horizontal/q3_bayes_flowchart_bw_horizontal.png}
  \caption{根据观测反馈逐步更新的搜索、定位与清除流程}
  \label{fig:q3-flow}
\end{figure}

\subsubsection{频道状态与贝叶斯更新}

以 $\mathcal H_t$ 表示第 $t$ 次决策前的检测和清除记录。
每个频道分别记录是否已发现、是否已清除或已排除，并保存其几何可能区域 $P_{c,t}$。
对于已发现源，位置概率用于比较补测和清除动作；对于未知频道，还需估计其存在概率，以安排搜索次序。
两类概率都不代替几何判据：概率较低的区域仍可能包含源，不能仅据此删除。

\textbf{先用反馈约束位置。}正常测向给出示向楔形，并排除距测点不超过 $5\,\mathrm m$ 的位置；
无信号说明源若存在，其距离超过有效接收半径；距离过近将源限制在 $5\,\mathrm m$ 内。
清除失败则排除清除点周围 $20\,\mathrm m$ 的范围，只有成功反馈才将该源记为已清除。
这些信息与已有区域逐次相交或相减，得到新的可能范围。

同一个接收半径必须解释此前的全部接收记录。
例如，某候选位置距一次有信号测点 $1200\,\mathrm m$、距一次无信号测点 $1300\,\mathrm m$，
则要求 $1200\leq R_c<1300$；若另一条有信号记录要求 $R_c\geq1400$，该候选位置便不相容。
记正常测向和无信号记录的编号集合分别为 $\mathcal I^+_{c,t}$、$\mathcal I^-_{c,t}$，
对候选位置 $\boldsymbol z$，允许半径的上下限为
\[
  \begin{aligned}
  \underline R_{c,t}(\boldsymbol z)
    &=\max\!\left\{1000,\max_{i\in\mathcal I^+_{c,t}}
        \|\boldsymbol z-\boldsymbol p_i\|\right\},\\
  \overline R_{c,t}(\boldsymbol z)
    &=\min\!\left\{1500,\min_{i\in\mathcal I^-_{c,t}}
        \|\boldsymbol z-\boldsymbol p_i\|\right\}.
  \end{aligned}
\]
某类记录为空时，仅保留相应的题设边界。

\textbf{再计算位置权重。}沿用面积均匀的位置先验和不同检测位置的均匀测角误差假设，
并取与位置独立的均匀半径先验。先检查每个候选位置是否符合此前的反馈：
从各测点看向该位置的方向应落在相应示向度的误差范围内，正常测向时距离还应超过 $5\,\mathrm m$；
收到距离过近反馈时，该位置应在对应测点的 $5\,\mathrm m$ 内；清除失败时，则应在清除点的 $20\,\mathrm m$ 外。

用 $\operatorname{Bool}_{c,t}(\boldsymbol z)$ 表示检查结果：上述条件全部满足时取 $1$，任一条件不满足时取 $0$。
在均匀测角误差假设下，角度反馈只用于排除误差范围外的位置，不改变范围内各候选位置的相对权重。

对面积为 $A_\ell$、代表点为 $\boldsymbol z_\ell$ 的积分单元，
再乘以该位置允许的接收半径占比，得到未归一化权重
\begin{equation}
  \widetilde w_\ell
  =A_\ell\,
    \frac{\overline R_{c,t}(\boldsymbol z_\ell)-\underline R_{c,t}(\boldsymbol z_\ell)}{500}
    \,\operatorname{Bool}_{c,t}(\boldsymbol z_\ell),
  \qquad w_\ell=\frac{\widetilde w_\ell}{\sum_k\widetilde w_k}.
  \label{eq:q3-position-weights}
\end{equation}
若允许半径的上限不大于下限，则直接将该位置的权重置零。
因此，不符合反馈的位置被排除；保留位置的权重由单元面积和允许的半径区间长度共同决定，
再除以权重总和，得到各位置的概率。
权重由完整历史重新计算，同频道同地点的重复反馈只保留一次。

\textbf{最后更新未知频道的搜索优先级。}对尚未发现信号的频道，先根据其无信号历史计算存在概率，
再结合已发现源数，将总源数限制在 $10$ 至 $16$ 的范围内进行条件化。
这一调整影响频道搜索的先后顺序；而频道是否可以排除，仍取决于几何可能区域是否已被实际检测排空。

\subsubsection{候选动作与时间评价}

每次决策比较三类动作：尝试清除已发现源、补充检测以缩小定位范围，以及前往测站搜索未知频道。
选点时既考虑接近源的距离，也考虑侧向检测的交会效果和其他任务的位置，生成有限个候选动作。
评价的重点是：现在执行哪一步，能够使预计剩余任务耗时更少。

\textbf{尝试清除。}对每个候选清除点，将其 $20\,\mathrm m$ 范围内的位置概率相加，
估计本次清除成功的机会。成功时可以结束对该源的处理；失败时还需继续定位和接近源。
因此，评分同时考虑到点移动、本次清除操作和失败后的预计处理时间，
再与继续检测的方案比较。若整个可能区域都在清除范围内，则可依据几何条件保证清除。

\textbf{补充检测。}沿用第二问的反馈预测，分别考虑正常示向、距离过近和无信号三类结果。
对每种结果更新位置概率，估计之后继续接近并清除该源需要的时间，再按反馈概率加权平均。
将这一预计时间与本次移动、检测及必要的切频时间相加，得到补测方案的局部费用。
例如，侧向补测虽然增加了一次检测，却可能减少后续寻找和清除失败的耗时；
是否值得补测，由两种方案的预计总时间决定。

\textbf{覆盖搜索。}对未知频道，估计前往候选测站并扫描所分配频道的时间。
若同样的扫描可在已有清除停留点完成，则将两种安排的行程一并比较，
在满足覆盖要求的前提下减少专程往返；各频道的检测和切频仍分别计费。

三类动作统一按时间评价。记候选动作为 $a$，其评分写为
\begin{equation}
  \widehat C_t(a)=\widehat T_{\mathrm{local}}(a)
    +\frac{\widehat L_{\mathrm{rem}}(a)}{5}
    +\widehat T_{\mathrm{rem,op}}(a).
  \label{eq:q3-score}
\end{equation}
第一项为上述局部费用：清除或补测动作计入本次移动及继续处理当前源的预计时间，
覆盖搜索动作计入到站移动和所分配频道的扫描时间。
第二项为此后访问其余源代表点与覆盖测站的预计移动时间，路程由下一节的联合路线计算。
第三项为尚未计入前两项的其余任务操作时间，包括其他源的定位清除和剩余频道扫描，避免重复计费。

后续路线从预计处理位置衔接：清除和覆盖搜索使用动作执行点，
补测后继续处理当前源时使用该源的代表位置。尚未精确定位的源以其位置后验均值近似。
算法优先执行评分较小的候选动作，收到真实反馈后重新评价。
其中，移动速度和操作时间来自题设，反馈概率来自位置模型，
失败恢复时间及未来访问位置则采用近似估计；式~\eqref{eq:q3-score} 因而用于指导选点，不能保证整个任务全局最优。

\subsubsection{搜索与清除的路线统筹}

将已发现源的代表点与未知频道的覆盖测站共同安排为开放路线。
尚未充分定位的源以位置后验均值作为代表点，具备保证清除条件时改用安全清除点；
未知频道的剩余区域按角向分区组织为搜索任务，并分配检测位置和扫描频道。

图~\ref{fig:q3-route-coordination} 展示两种减少绕行的方式。
在同一组构造位置中，清除点 $A$ 已能完整覆盖待搜索区域 $D_1$，
因此可在此扫描相应频道，省去独立测站 $U$。
对仍需保留的测站 $V$，则在完整覆盖 $D_2$ 的条件下移至 $V'$，缩短与前后访问点的连接路程。
调整后均重新检查覆盖条件，只接受仍可完成搜索且路程缩短的方案。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{data/q3_route_coordination/q3_route_coordination.png}
  \caption{搜索与清除的路线统筹：合并停留点与调整测站位置}
  \label{fig:q3-route-coordination}
  \par\smallskip
  \begin{minipage}{0.96\linewidth}\small
    注：三图使用相同的清除位置和待搜索区域，虚线圆弧为测站的覆盖边界，灰色折线为调整前路线。
    路程按构造坐标计算，仅用于说明机制。
  \end{minipage}
\end{figure}

共用停留点节省的是移动路程，各频道仍分别切换和检测；路线预测按每频道约 $6\,\mathrm s$ 计费，
实际执行按当前频道计算切频次数。每次反馈后更新剩余任务并重新安排路线，
扫描完成与源已清除均以实际反馈为准。

\subsubsection{策略执行与停止条件}

上述评分和路线规划确定了下一步去哪里、做什么，执行时还需判断是否能够清除，以及何时可以结束搜索。
这一过程我们采用表~\ref{tab:q3-parameters} 的参数进行位置积分、反馈预测和路线计算。

\begin{table}[htbp]
  \centering
  \caption{算法主要参数}
  \label{tab:q3-parameters}
  \small
  \begin{tabularx}{0.96\linewidth}{lX}
    \toprule
    参数 & 设置 \\
    \midrule
    位置积分网格 & $24\times24$，必要时加密 \\
    预测示向度档宽 & $1^\circ$ \\
    初始频道存在概率 & $0.65$，随观测和源数约束更新 \\
    覆盖任务 & $7$ 个角向分区，检查半径 $997\,\mathrm m$，预留近似裕量 \\
    路线求解 & 不超过 $12$ 点时用精确动态规划\cite{held1962dynamic}，否则用 2-opt 等局部改进\cite{croes1958method} \\
    后备模式触发 & 全局动作数达到 $600$ 或实际剩余时间不足 $30\,\mathrm s$ \\
    \bottomrule
  \end{tabularx}
\end{table}

执行清除前，先检查当前保守可能区域是否满足
\begin{equation}
  P_{c,t}\subseteq B(\boldsymbol q,20),
  \label{eq:q3-safe-clear}
\end{equation}
满足时即可保证清除，并在允许范围内选择靠近机器狗的位置；否则继续按时间评分比较尝试清除与补测。
执行后，成功反馈用于移除该源，失败反馈用于缩小可能区域，随后重新安排剩余任务。

若位置网格权重全为零，先加密或转入后备搜索，不据此排除频道。
达到表中的后备条件时，对已发现源安排有限网格清除尝试，对未知频道继续覆盖检测。
这一过程持续到已发现源全部清除、其余频道均被实际无信号记录排除，或已清除 $16$ 个不同频道的源。
若此前实际运行时间已耗尽，则记为未完成案例，纳入后续评价。

\subsubsection{结果评价与全知者参照}

我们搭建了本地模拟器，以开展快速的批量测试。
模拟器按题设生成检测与清除反馈并累计各项耗时，且不会泄露干扰源的真实位置。

我们选取40张未参与调参的随机地图，以及24组压力场景对我们的算法进行测试。
随机地图中的源按圆域面积均匀分布；压力场景组合了均匀、边界、聚集和近距离分布。
两批共64局全部清除并确认无遗漏，累计清除857个源，平均耗时见表~\ref{tab:q3-local-oracle}。

\begin{table}[!ht]
  \centering
  \caption{本地批量模拟结果与全知者平均耗时参照}
  \label{tab:q3-local-oracle}
  \small
  \renewcommand{\arraystretch}{1.2}
  \begin{tabularx}{\linewidth}{Xcccc}
    \toprule
    方案与场景 & 地图数 & \shortstack{清除数/\\总源数} & \shortstack{平均移动时间\\/(s/源)} & \shortstack{平均总时间\\/(s/源)} \\
    \midrule
    本文算法：随机地图 & 40 & 545/545 & 161.51 & 216.01 \\
    本文算法：压力场景 & 24 & 312/312 & 149.04 & 207.46 \\
    全知者：随机地图 & 100000 & --- & 136.52 & 141.52 \\
    \bottomrule
  \end{tabularx}
  \par\smallskip
  \begin{minipage}{\linewidth}\small
    注：各项平均时间均由该组总时间除以总源数得到，均为模拟任务耗时。
    全知者的总时间只含最短移动时间和每源5秒清除操作，未执行未知源搜索任务。
  \end{minipage}
\end{table}

图~\ref{fig:q3-execution-case} 展示一个本地模拟案例的实际执行过程。
该局清除全部15个源并确认无遗漏，总耗时 $2730.53\,\mathrm s$，移动距离 $10142.65\,\mathrm m$，
其中移动是主要耗时，说明路线安排是进一步缩短任务时间的重要环节。

\begin{figure}[!ht]
  \centering
  \includegraphics[width=\linewidth]{data/q3_execution_case/q3_execution_case.png}
  \caption{本地模拟案例的实际执行轨迹与虚拟耗时构成}
  \label{fig:q3-execution-case}
\end{figure}

进一步地，为给任务耗时提供参照，我们设置了预先知道全部源位置的“全知者”。
它从原点出发访问所有源坐标，无需返回，因而可用动态规划算法求解开放式旅行商问题，获得最短移动路程。
在源数等概率取10--16、源位置按圆域面积均匀分布的十万张随机地图上，
全知者的平均纯移动时间为 $136.52\,\mathrm{s}/\text{源}$；
加上每源一次光学定位与清除的 $5\,\mathrm s$，得到 $141.52\,\mathrm{s}/\text{源}$ 的参照值。
该参照不包含搜索、测向和切频，也要求到达源坐标；但我们承认，实际任务只需进入源周围20米，因此它并不是全知者的严格最优值，这里仅为作数值上的直观比较。

上述批量模拟补充了不同场景下的表现，三次正式测试另按表~\ref{tab:q3-official} 记录，

\begin{table}[!ht]
  \centering
  \caption{问题三正式测试结果（待填）}
  \label{tab:q3-official}
  \small
  \renewcommand{\arraystretch}{1.25}
  \begin{tabularx}{0.96\linewidth}{Xccc}
    \toprule
    测试案例编码 & 清除干扰源个数 & \shortstack{平均定位清除\\时间/(s/源)} & 程序运行时间/s \\
    \midrule
    测试1：待填 & --- & --- & --- \\
    测试2：待填 & --- & --- & --- \\
    测试3：待填 & --- & --- & --- \\
    \bottomrule
  \end{tabularx}
\end{table}
\FloatBarrier

\subsection{问题四模型建立与求解}
\label{sec:q4}

问题三已形成“后验更新—反馈预测—时间评价—执行反馈”的连续决策过程。
本问面对全向与定向源混合的情形，决策目标保持不变，但预测反馈时必须增加源类型与发射朝向——
一次无信号既可能来自距离过远，也可能来自发射背面。
因此，先建立适用于定向辐射的联合排除与覆盖条件，再以扩展后的后验分布预测反馈、比较行动时间。

\subsubsection{从定向接收到多点联合排除}

设频道 $c$ 的源位于 $\boldsymbol z$，接收半径为 $R_c$，定向源的发射方向用单位向量 $\boldsymbol u_c$ 表示。
测点 $\boldsymbol p$ 收到该源信号的条件为
\begin{equation}
  \|\boldsymbol p-\boldsymbol z\|\leq R_c,\qquad
  \boldsymbol u_c^{\mathsf T}(\boldsymbol p-\boldsymbol z)\geq0.
  \label{eq:q4-reception}
\end{equation}
两式分别限制接收距离与发射前方，等号包含辐射边界；全向源只受距离限制。
一次无信号无法区分超出接收距离与位于发射背面，因而不能直接排除测点附近的圆域。
但若多个测点围住候选源位置，且均在最小接收距离以内，它们便不可能同时处于发射背面。

据此，设同一频道在三个不共线测点 $\boldsymbol p_i,\boldsymbol p_j,\boldsymbol p_k$ 均无信号，定义
\begin{equation}
  E_{ijk}=\operatorname{conv}\{\boldsymbol p_i,\boldsymbol p_j,\boldsymbol p_k\}
  \cap B(\boldsymbol p_i,1000)\cap B(\boldsymbol p_j,1000)\cap B(\boldsymbol p_k,1000).
  \label{eq:q4-exclusion}
\end{equation}
其中，凸包限制候选位置被三点围住，三个闭圆盘保证其到各测点的距离均不超过 $1000\,\mathrm m$。
若源位于 $E_{ijk}$ 内，可写成 $\boldsymbol z=\sum_a\lambda_a\boldsymbol p_a$，
其中 $\lambda_a\geq0$、$\sum_a\lambda_a=1$，从而
\[
  \sum_{a\in\{i,j,k\}}\lambda_a\boldsymbol u_c^{\mathsf T}(\boldsymbol p_a-\boldsymbol z)=0.
\]
如果三点均在背面，上式左侧应为负，产生矛盾；若为全向源，三点则都应收到信号。
因此，三条无信号记录可以排除 $E_{ijk}$ 内存在该频道的源。
每增加一条实际无信号记录，就与该频道已有记录组成三点组合，累计扣除可排除区域。
这一判断不依赖预先知道源的类型或朝向，是后续全域搜索的依据。

\subsubsection{覆盖全域的测点布局}

局部排除判据给出了布点目标：使目标圆域内每个位置，都处于一组近邻测点的凸包内，
且到这些测点的距离均小于最小接收半径。
这样，存在的源必能在至少一个测点被发现；若某频道在全部相应测点均无信号，便可确认该频道不存在源。

为兼顾中心与边缘的覆盖，采用“中心点＋两层环形测点”的布置，如图~\ref{fig:q4-coverage} 所示。
中心点与内圈测点围住内部区域，外圈从目标域外侧补足边缘覆盖，
使靠近边界且朝外发射的源也能被检测到。
本文采用经覆盖核验的21点可行布局：内圈8点、外圈12点，分别按 $45^\circ$ 和 $30^\circ$ 等角布置，
两圈均从正东方向起排，名义半径分别为 $998\,\mathrm m$ 和 $1866\,\mathrm m$，坐标取整。
这些数值是所用布局的参数，覆盖性由下述检查确认，不据此声称测点数最少。
题设仅限制源的分布范围，外圈测点可位于半径 $1800\,\mathrm m$ 的目标圆域之外。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{data/q4_coverage_geometry/q4_coverage_geometry.png}
  \caption{联合排除原理与覆盖布局。左图为构造示意，阴影区域同时满足凸包与接收距离约束；
  右图为实际采用的21点布局，虚线表示两圈测点的布置半径。}
  \label{fig:q4-coverage}
\end{figure}

覆盖检查采用方格逐级四分。对与目标域相交的每个方格，寻找一组测点，
使其四角均落在这组测点的凸包内，且到各测点的距离均小于 $999\,\mathrm m$；未通过则继续细分。
由于凸包与圆盘均为凸集，四角通过即可保证整个方格通过，前述投影证明也适用于三个以上的测点。
独立核验共确认 $3832$ 个方格，最大角点距离为 $998.79\,\mathrm m$，覆盖目标域及其边界。
因此，该布局提供了完整搜索的几何依据。

覆盖测点是尚未完成的搜索任务，并不要求每场均依次走完21点。
随着频道被发现、清除或排除，可删除不再需要的检测任务，或利用清除停留点替代附近测点。
每次调整都重新检查各频道的覆盖要求；用于确认频道不存在的证据，则只能来自已经执行的无信号检测。

\subsubsection{贝叶斯更新与行动选择}

在前述覆盖布局的基础上，沿用问题三的搜索清除流程，将源类型和发射朝向加入贝叶斯更新。
设源位置在目标圆域内均匀分布，接收半径在 $[1000,1500]\,\mathrm m$ 内均匀分布，
全向与定向源的先验概率各为 $1/2$，定向源朝向在整周内均匀分布；各未知量初始独立，实际值在任务中固定。
正常示向、距离过近及清除反馈仍按几何约束更新位置，其中示向楔形半角计入舍入后取 $1.005^\circ$。

对每个候选位置，用该源的全部历史记录共同筛选可能的类型、半径和朝向，
按先验概率加权并计入示向读数的概率，得到历史反馈在该位置下的似然 $q_\ell$。
将可行区域划分为面积为 $A_\ell$ 的网格单元，归一化得到位置权重
\begin{equation}
  w_\ell=\frac{A_\ell q_\ell}{\sum_j A_jq_j}.
  \label{eq:q4-position-weight}
\end{equation}
由此预测各候选测点出现正常示向、距离过近与无信号的概率，使选点同时考虑几何交会效果和收到信号的可能性。
计算采用随可行区域旋转的 $16\times16$ 网格及裁剪面积，预测示向先按 $10^\circ$ 分档筛选，再按 $2^\circ$ 分档细算。

随后按式~\eqref{eq:q3-score} 比较各动作的预计完成时间，计入移动、切频、检测、清除及剩余任务，
执行下一动作后依据实际反馈重新规划。停留点复用须满足本问的联合覆盖条件，各频道仍逐一检测；
清除判据沿用问题三，即完整可行区域可被半径20米的圆覆盖时能够保证清除，否则以实际清除反馈为准。

若网格权重全为零，先加密至 $32\times32$；仍无有效权重则保留几何可行区域，转入后备搜索。
未知频道继续按联合覆盖条件排查，不以概率或数值权重过小判定无源。
当20个频道均已清除或被实际记录排除，或已成功清除16个不同频道的源时结束；先达到时限则记为未完成。

\subsubsection{完整任务的实验结果}

与问题三同样，我们使用本地实验进行测试，随机地图包含全向与定向源。
采用上述21点布局和统一的定位、调度规则，在16张随机地图与14张压力地图上运行。
压力地图覆盖边界朝外发射、切向发射、源聚集、近距离及背向检测等情形，
并加入接收半径边界值、极端测角误差或空间相关误差。
% 数据沿用原稿批次20260913-013804-paper-q4-recheck中21点方案的30场结果；本次为表述重组，未重跑实验。
% 随机种子800--815，压力种子9100--9113；不混用其他批次，不将本次改写记为新增验证。

30场均清除全部源并满足停止条件，未出现超时或运行失败，分组结果见表~\ref{tab:q4-completion}。
总虚拟时间按式~\eqref{eq:q3-time} 计入移动、切频、检测及清除，
其中包含清除已发现源后继续检查未知频道的时间。

\begin{table}[htbp]
  \centering
  \caption{问题四本地完整任务结果}
  \label{tab:q4-completion}
  \begin{tabular}{lrrr}
    \toprule
    地图组 & 场数 & 完整完成场数 & 平均总虚拟时间/s \\
    \midrule
    随机地图 & 16 & 16 & 5670.72 \\
    压力地图 & 14 & 14 & 5721.44 \\
    合计 & 30 & 30 & 5694.39 \\
    \bottomrule
  \end{tabular}
  \par\smallskip
  \parbox{0.94\linewidth}{\footnotesize 注：各组均清除全部干扰源；合计均值按30场等权计算。
  压力地图为人为构造，用于检查特殊情形，不代表正式测试的地图分布。}
\end{table}

30场平均移动路程为 $20.384\,\mathrm{km}$，平均检测261次、失败清除2次，
单场实际计算时间平均为 $5.34\,\mathrm s$。
按移动速度换算，平均移动耗时约 $4076.8\,\mathrm s$，是本批任务耗时的主要部分，
说明评价选点时有必要同时计入前往测点和后续清除的路程。

三次正式测试仍需按题设执行，结果与导出日志另行记录，表~\ref{tab:q4-official} 保留待填。
\begin{table}[htbp]
  \centering
  \caption{问题四正式测试结果（待填）}
  \label{tab:q4-official}
  \small
  \renewcommand{\arraystretch}{1.25}
  \begin{tabularx}{0.96\linewidth}{Xccc}
    \toprule
    测试案例编码 & 清除干扰源个数 & \shortstack{平均定位清除\\时间/(s/源)} & 程序运行时间/s \\
    \midrule
    测试1：待填 & --- & --- & --- \\
    测试2：待填 & --- & --- & --- \\
    测试3：待填 & --- & --- & --- \\
    \bottomrule
  \end{tabularx}
\end{table}
\FloatBarrier


\section{模型评价与展望}
\label{sec:discussion}

\subsection{模型的主要优点}

\textbf{定位指标与清除条件直接对应。}
半区域直径描述两个可行位置之间的最大跨度，最小覆盖圆半径描述选择最优估计位置后仍可能存在的最大定位误差。
因此，模型既能评价定位范围，也能判断是否存在一个清除点，使全部可行位置均处于其清除范围内，从而区分保证清除与试探性清除。

\textbf{观测价值与后续行动能够共同评价。}
模型通过预测候选测点的不同反馈，在第二问中评价期望定位范围，
在第三、四问中进一步评价后续任务的预计耗时。每次获得真实反馈后重新选点和安排路线，使补测、清除与未知频道搜索能够共同考虑。
清除停留点还可兼作检测位置，为减少独立搜索行程提供机会。

\textbf{定向搜索具有明确的排除依据。}
定向源的搜索完成具有几何依据。
多点联合排除利用接收距离与测点包围关系，无需预先确定源的朝向；
覆盖布局进一步将局部排除条件扩展到整个目标域。
概率权重用于安排行动顺序，频道排除则依据实际无信号记录及相应覆盖条件，避免仅凭低概率判断源不存在

\subsection{局限性与改进方向}

首先，概率预测依赖位置先验、接收半径分布、源类型比例及测角误差假设。
当实际分布或误差规律偏离这些设定时，反馈预测与行动排序可能出现偏差，进而影响任务耗时。
第二问的选点结果以给定接收半径为条件，当仅知其取值范围时，仍需明确未知半径如何参与选点评价。
后续可固定地图与实际观测误差，考察不同先验设定对行动选择的影响，并在不同误差规律下检验策略表现。
若实际测角误差超出预设上界，几何约束也可能排除真实位置，需要重新确定误差范围或处理异常观测。

其次，位置积分与反馈分档会引入数值近似，有限候选点搜索也可能遗漏更优测点。
第三、四问采用局部时间估计与滚动决策，尚不能保证整场任务的全局最优；动态规划的最优性仅针对当前固定节点的访问顺序。
后续可进一步检查候选搜索范围与离散精度对选点结果的影响，并在相同地图和误差条件下，
分别关闭停留点复用、测站调整等组件，量化其对任务耗时的贡献。
同时，可结合同图全知者的移动代价参照，分析路线安排的改进空间。

最后，当前结果主要来自本地模拟，有限案例尚不足以评价更广泛场景下的性能，第三、四问的正式测试结果仍需依据对应执行记录补充。
后续评价应同时报告完整完成情况与耗时分布，并分析未完成或耗时较高的案例。
推广到真实环境时，还需考虑机器狗自身定位误差、通行障碍及加减速和转向耗时，
以可通行路径和实际运动代价替代题设中的直线匀速模型。

\begin{thebibliography}{99}

\bibitem{boyd2004convex}
Boyd S, Vandenberghe L.
Convex Optimization[M].
Cambridge: Cambridge University Press, 2004: 34, 36.
\href{https://web.stanford.edu/~boyd/cvxbook/}{作者公开版本}.

\bibitem{deberg2008geometry}
de Berg M, Cheong O, van Kreveld M, et al.
Computational Geometry: Algorithms and Applications[M].
3rd ed. Berlin: Springer, 2008.
\newline
\href{https://doi.org/10.1007/978-3-540-77974-2}{DOI: 10.1007/978-3-540-77974-2}.

\bibitem{gelman2013bayesian}
Gelman A, Carlin J B, Stern H S, et al.
Bayesian Data Analysis[M].
3rd ed. Boca Raton: CRC Press, 2013.
\href{https://sites.stat.columbia.edu/gelman/book/}{作者公开版本}.

\bibitem{held1962dynamic}
Held M, Karp R M.
A Dynamic Programming Approach to Sequencing Problems[J].
Journal of the Society for Industrial and Applied Mathematics, 1962, 10(1): 196--210.
\href{https://doi.org/10.1137/0110015}{DOI: 10.1137/0110015}.

\bibitem{croes1958method}
Croes G A.
A Method for Solving Traveling-Salesman Problems[J].
Operations Research, 1958, 6(6): 791--812.
\href{https://doi.org/10.1287/opre.6.6.791}{DOI: 10.1287/opre.6.6.791}.

\end{thebibliography}

\end{document}
