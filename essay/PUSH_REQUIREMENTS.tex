% essay目录整理与推送要求
% 本文件是维护说明，不是论文正文；不要input到论文中，也不要作为主文件编译。
%
% 1. essay根目录只允许*.tex文件与data/目录。
%    当前论文主文件：essay_Q1_V2_Q2_V1_Q3_V1.tex。
%    不提交编译产物（PDF、aux、log、out等）、压缩包、缓存及历史备份。
%
% 2. 一张独立PNG对应data/中的一个语义明确的小目录。
%    多面板组合图若保存为一个PNG，视为一张图；左右独立PNG分别建目录。
%    每目录包含论文实际使用的PNG和generate.py；必要数值输入、辅助代码可随图保存。
%    脚本应从自身目录定位输入输出，不能依赖个人电脑绝对路径。
%    默认只生成本目录对应的一张PNG，不连带生成其他论文图或无关格式。
%
% 3. 数值算法的正式实现放src/Q2--Q4；essay/data中的代码负责图的复现。
%    可随图保存独立运行所必需的数值模块，但应说明其来源、保持模型一致。
%    热力图默认读取冻结CSV/JSON，python generate.py即可重绘。
%    热力图增加--recompute才重新计算；不将重绘等同于重新验证模型。
%
% 4. 插图引用必须使用data/<图目录>/<图片名>.png相对路径。
%    不引用临时文件、微信目录、下载目录或工作区外文件。
%    更新PNG时同步更新生成代码和必要输入，并核对图注、参数及正文数值。
%    未经实测不得填入算例成绩；未完成的论文段落维持明确占位。
%
% 5. 推送前检查：所有图片引用存在；生成代码可运行；论文用XeLaTeX编译两遍。
%    编译输出放essay之外；检查缺图、未定义引用、溢出和相关页面版式。
%    只提交这次有意修改的文件，不混入账号、接口日志或其他任务的修改。
%
% 6. 当前经用户明确接受的例外：以下三图只有PNG，源码待补，保留原图不重绘。
%    data/q2_initial_region_v1/
%    data/q2_annular_grid_v3/
%    data/q2_reception_clipping_v4/
%    各目录以SOURCE_PENDING.tex记录缺失状态；不得提供假生成代码冒充可复现。
%    后续新增图仍按第2条提供真实生成代码。
%
% 7. 当前生成依赖：Python 3.10+，numpy、scipy、shapely>=2、matplotlib。
%    中文绘图原样使用Microsoft YaHei/SimHei；跨平台替换字体后版式可能变化。
%    Q2热力图model_core.py来自src/Q2（论文固定R0版本），保留数值定义。
%    Q1 n=10的generate.py默认种子20260911，默认200次验证，仅输出论文几何PNG。
%
% 2026-09-12整理：保留现有正文（包括未完成框架），只重定位9张插图的引用。
