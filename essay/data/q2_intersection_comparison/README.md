# 第二问：三种选点的交会结果展示

本图展示固定构造情形的区域更新，不承担随机验证或平均性能评价。

首测点为(0,0)米，示向度为0度；源位置为(900,0)米，固定接收半径1200米。首测误差为0度，三个第二测点的误差均为+0.35度。第二测点依次为(300,0)、(0,300)、(840,495)米。

`generate.py`按当前正文的固定半径模型计算交会区域和最小覆盖圆，输出一张三列两排PNG、观测CSV、结果JSON及交会区域GeoJSON。上排坐标范围统一，下排局部窗口也统一；左下图看不到完整覆盖圆，因此正文图注注明完整圆见左上图。

`model_core.py`复制自`src/Q2/model_core.py`且未修改。圆弧近似沿用原计算设置。脚本核对源位置满足首次与第二次观测约束，且计算圆覆盖交会多边形的全部凸包顶点。单次结果的半径与正文期望指标J分别标示。

运行：`python generate.py`。所有输入由脚本中的固定构造给出，不使用随机抽样；输入输出均相对脚本目录定位。
